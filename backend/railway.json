{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "preDeployCommand": "php artisan config:clear && php artisan migrate --force && rm -rf public/storage && php artisan storage:link --relative",
    "startCommand": "if [ -n \"$START_COMMAND\" ]; then $START_COMMAND; else php artisan serve --host=0.0.0.0 --port=$PORT; fi"
  }
}
