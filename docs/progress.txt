# Progress Log

## [2026-06-04] Phase 1 Completed
- Set up Docker infrastructure with isolated services: backend, nginx, postgres (postgis), redis, frontend, worker, and scheduler.
- Configured PHP 8.4-FPM with necessary extensions.
- Configured Next.js frontend container with hot reloading.
- Orchestrated Laravel Queue Worker and Scheduler.
- Verified all services are operational.

## [2026-06-04] Phase 2 Completed
- Downgraded Laravel to v12 for better compatibility with Filament and PRD requirements.
- Installed and configured Laravel Sanctum for API authentication.
- Installed and configured Filament PHP v3 for the administration panel.
- Implemented core data architecture:
    - `users` updated with check-in and push token fields.
    - `circles` and `circle_user` pivot table.
    - `emergency_contacts`.
    - `current_locations` and `location_histories` with PostGIS support.
- Created API endpoints for Registration, Login, and Daily Check-in.
- Automated API documentation with L5-Swagger using PHP 8.4 attributes.
- Developed inactivity verification system:
    - Console command `checkins:verify-inactivity` to detect inactive users.
    - Queued job `SendInactivityAlerts` for Push and Email notifications.
    - Scheduled verification to run every 30 minutes.
- Built basic Filament resources for Users, Circles, and Emergency Contacts.
- Verified end-to-end flow with seeders and manual command execution.

## [2026-06-04] Phase 3 Completed
- Integrated multi-region payment gateways: Stripe, Mercado Pago, and PayPal.
- Installed and configured Laravel Cashier for international Stripe subscriptions.
- Implemented Mercado Pago subscription flow using the official PHP SDK.
- Integrated PayPal Subscriptions for alternative international payments.
- Developed a unified `SubscriptionController` for generating checkout URLs across all providers.
- Created Webhook endpoints for Mercado Pago and PayPal to sync subscription status and `is_premium` flag.
- Updated `User` model with `Billable` trait and subscription status helper methods.
- Enhanced Filament Admin Panel to manage user subscription details and premium status.
- Verified subscription logic with Feature tests using mocked gateway services.
- Updated Swagger documentation with new subscription and checkout endpoints.

## [2026-06-04] Phase 4 Completed
- Integrated WhatsApp and SMS alerts for premium users using Twilio SDK.
- Created `WhatsAppServiceInterface` and `TwilioService` implementation.
- Updated `SendInactivityAlerts` job to:
    - Check for premium access before sending WhatsApp/SMS.
    - Send WhatsApp alerts to all emergency contacts.
    - Fallback to SMS if WhatsApp delivery fails.
- Configured Twilio in `services.php` and bound it in `AppServiceProvider`.
- Verified the logic with comprehensive feature tests (`WhatsAppAlertTest`).

## [2026-06-04] Phase 5 Completed (Backend)
- Implemented `POST /api/locations/update` endpoint for high-frequency tracking.
- Integrated PostGIS spatial queries to update `current_locations` and `location_histories`.
- Created `Geofence` and `GeofenceEvent` models and migrations.
- Developed `ProcessGeofencing` background job with:
    - Entry/Exit detection using `ST_DWithin`.
    - Notification spam prevention using event history.
    - Automated Push Notifications to circle members via Expo API.
- Verified backend tracking and geofencing with `GeofencingTest`.

## [2026-06-04] Phase 6 Completed
- Implemented `EmergencyAlert` model with UUID for secure public access.
- Updated `SendInactivityAlerts` job to generate emergency links in WhatsApp and Email notifications.
- Created `EmergencyAlertController` to serve public alert data and last known location via PostGIS.
- Developed Next.js dynamic route `/emergencia/[id]` with a professional crisis screen UI.
- Integrated `Leaflet` for real-time map visualization of the user's last coordinates.
- Verified backend logic with `EmergencyAlertTest` covering creation, retrieval, and expiration.
- Verified frontend integrity with linting.

## [2026-06-04] Phase 7 Completed
- Implemented Email Verification via 6-digit OTP.
- Created `email_verifications` table and model.
- Updated `AuthController@register` to require verification before issuing tokens.
- Added `POST /api/verify-email` and `POST /api/resend-otp` endpoints.
- Implemented `OtpVerificationMail` using Markdown and Queues.
- Updated Swagger documentation with new authentication endpoints.
- Verified entire flow with `EmailVerificationTest`.
- Integrated OTP flow into the Expo mobile app and Next.js web frontend:
    - New `verify-email` screens with 6-digit input.
    - Updated `register` flows to navigate to verification.
    - Adjusted routing and navigation state to handle the verification phase.

## [2026-06-09] Mobile Environment Stabilization
- Upgraded Expo SDK to v54.0.0 to ensure compatibility with Expo Go and latest dependencies.
- Updated React to v19.1.0 and React Native to v0.81.5.
- Synchronized all Expo modules (`location`, `task-manager`, `notifications`, etc.) to versions compatible with SDK 54.
- Verified background location tracking and manual check-in functionality on real devices via Expo Go.
- Fixed dependency conflicts with `react-native-reanimated` and `expo-router` resulting from the SDK upgrade.

## [2026-06-09] Inactivity Alert System & UI Refactor
- **Customizable Thresholds:** Implemented `checkin_interval_hours` in `users` table and updated backend logic to support user-defined inactivity periods.
- **Enhanced Emergency Contacts:** Added `relationship` and `is_active` fields to `emergency_contacts`.
- **Mailable Implementation:** Created `InactivityAlertMail` with professional Markdown design and integration with Mailpit.
- **Dev-Testing Mode:** Added a mechanism to treat intervals as minutes and run the scheduler every minute in the `local` environment for rapid testing.
- **UI Modularization:** 
    - **Web:** Refactored Dashboard into two distinct modules: "Mi Bienestar Diario" (Check-in, Contacts, Settings) and "Rastreo y Círculos" (Map, Circles).
    - **Mobile:** Redesigned Home screen with independent cards for Wellbeing and Tracking, including embedded configuration buttons.
- **Bug Fixes:** Resolved timezone discrepancies in web reports by implementing client-side date formatting.
- **Verification:** Verified end-to-end alert flow with logs, Mailpit, and automated feature tests.

## [2026-06-10] Password Visibility Toggle
- Added password visibility toggles (eye/eye-off icons) in both login and register screens.
- Supported both Web (Next.js) and Mobile (React Native / Expo) platforms.
- Integrated Lucide icons for React Native and custom inline SVGs for Next.js.

## [2026-06-10] UX Improvement: Web Toast Notifications
- Replaced native browser `alert()` windows in the Web Dashboard with custom designed, floating Toast notifications.
- Added support for auto-dismissible Success/Error notifications matching the site color scheme and layout.
- Designed and integrated a persistent status banner ("Protegido y a Salvo", "Reporte Vencido", "Sin Reportes") matching the security state of the user.
- Implemented a prominent, modern congratulations banner (emerald-teal gradient) that dynamically displays when a user successfully checks in.

## [2026-06-10] UX Improvement: Mobile Wellbeing Status Banner
- Added the same persistent wellbeing status banner ("Protegido y a Salvo", "Reporte Vencido", "Sin Reportes") to the Mobile home screen.
- Integrated automated fetch calls on mount and check-in to keep user state in Mobile synchronised.
- Added native Pull-to-Refresh (`RefreshControl`) support on Mobile Home screen to allow users to pull down to reload active state and tracking status.

## [2026-06-10] Automatic Emergency Alert Resolution & Privacy
- Modified `POST /api/check-in` in the backend (`CheckInController`) to automatically update active user emergency alerts to `resolved` status.
- Added `emergencyAlerts` relationship to the `User` model.
- Restructured `EmergencyAlertController@show` to completely hide location coordinates (returning `null`) when the status of the alert is `resolved`.
- Updated the public Next.js emergency page (`EmergencyClientPage` at `/emergencia/[id]`) to show a prominent green reassurance banner when the alert is resolved.
- Deactivated map rendering in the public emergency view when the alert is resolved, replacing it with a secure padlock view explaining the privacy protection.
- Handled theme coloring transition in header to green and updated vivos badge to `RESUELTA` when resolved.
- Verified all additions with unit and integration tests.

## [2026-06-15] Fix Frontend Leaflet Dependency & Mobile Authrush Checks
- Fixed missing `leaflet` and `react-leaflet` dependency issue in the frontend Docker volume.
- Modified `docker-compose.yml` to automatically run `npm install` before launching Next.js in the frontend container.
- Resolved mobile start-up Axios 401 error by displaying an `ActivityIndicator` loading state in `_layout.tsx` while authentication details are loading, and guarding the `fetchUserData` call in `index.tsx` so it only runs once the user is loaded.
- Fixed 8 TypeScript type check compilation errors in the mobile codebase (`index.tsx`, `settings.tsx`, `verify-email.tsx`, `animated-icon.tsx`, `collapsible.tsx`, `AuthContext.tsx`, and `use-theme.ts`), achieving clean build execution (`npx tsc --noEmit` exits with 0).
- Implemented E.164 phone formatting feature: added model-level sanitization mutators in backend `User` and `EmergencyContact` models, added phone prefix validation (`+`) in web (`EmergencyContacts.tsx`) and mobile (`contacts.tsx`) components, and added helper instructions for users.
- Implemented specific email format validation checks on both client-side and server-side in Web (`EmergencyContacts.tsx`) and Mobile (`contacts.tsx`) views, replacing generic error messages with clear, actionable user feedback.

## [2026-06-15] Check-in Logging & History (Web & Mobile)
- Created database migration for `check_ins` table to record check-in historical logs linked to users.
- Created `CheckIn` Eloquent model with user relationship, and updated `User` model with `checkIns` relation.
- Updated `CheckInController@store` to log a check-in history record on successful check-in.
- Implemented `GET /api/check-ins` endpoint with Swagger annotations to retrieve the last 20 check-in logs.
- Added comprehensive integration tests in `CheckInHistoryTest` verifying logging and retrieval endpoints.
- Updated Next.js `Dashboard.tsx` with check-in history state, fetching mechanism, and a premium "Historial de Reportes" card under Wellbeing.
- Updated Expo React Native `index.tsx` home screen to fetch and display the last 5 check-in logs in a modern compact card.
- Fixed minor pre-existing web ESLint warnings (unused variables, unescaped double quotes).

## [2026-06-15] Sleep Mode / Quiet Hours (Web, Mobile & Backend)
- Created database migration adding `quiet_hours_enabled`, `quiet_hours_start`, `quiet_hours_end`, and `timezone` to the `users` table.
- Added quiet hours properties to the `User` model and implemented the `isInQuietHours()` helper method to resolve midnight crossovers correctly.
- Created `PUT /api/settings/quiet-hours` API endpoint in `SettingsController` with Swagger annotations to update quiet hours preferences.
- Modified `checkins:verify-inactivity` and `checkins:send-reminders` console commands to skip users during quiet hours.
- Created robust feature integration tests in `QuietHoursTest` verifying the crossover logic and console command skipping.
- Expanded Next.js `SecuritySettings.tsx` to include the "Modo Sueño" section with a custom switch toggle and time inputs.
- Redesigned the Expo React Native `settings.tsx` screen into a scrollable layout including the "Modo Sueño" configuration section with switch and format-validated time inputs.

## [2026-06-15] Fix Timezone Validation in Sleep Mode Settings
- Resolved timezone validation error (`The timezone field must be a valid timezone.`) when saving or disabling Sleep Mode on Web.
- Implemented automatic timezone normalization in `SettingsController` to resolve non-canonical browser-supplied timezones (like `America/Buenos_Aires`) to canonical identifiers (like `America/Argentina/Buenos_Aires`) before validation.
- Restored `timezone` rule validation in backend SettingsController for stricter security constraints.
- Added integration test `test_user_timezone_is_normalized_automatically` to prevent regressions.
- Optimized mobile settings: wrapped settings screen in `KeyboardAvoidingView` to prevent inputs from being covered by the keyboard, and split quiet hours selection into separate Hour (HH) and Minute (MM) numeric inputs to ensure stable `number-pad` entry.

## [2026-06-17] Twilio Webhook for SMS/WhatsApp Check-in
- Added database migration defining the `allow_sms_whatsapp_checkin` boolean column in `users`.
- Updated the `User` model with the new column in `$fillable` and `casts()`.
- Created `PUT /api/settings/sms-whatsapp-checkin` API endpoint with Swagger/OpenAPI documentation.
- Created public webhook `POST /api/webhooks/twilio/message` to receive incoming SMS/WhatsApp replies from Twilio.
- Added E.164 phone parser, active user validation, check-in registration, emergency alert resolution, and TwiML XML responses.
- Covered all webhook flows with automated feature tests (`TwilioWebhookTest`).
- Exposed the `allow_sms_whatsapp_checkin` setting in the Filament Admin Panel (`UserResource`).
- Integrated settings toggle in Web Dashboard settings (`SecuritySettings.tsx`, `Dashboard.tsx`).
- Integrated settings toggle in Mobile application Settings screen (`settings.tsx`, `settings.ts`).
- Verified zero compilation and type-check errors across Web and Mobile frontends.

## [2026-06-17] Sequential Escalation of Inactivity Alerts & Reordering of Contacts
- Added database migration defining `priority` column in `emergency_contacts`, and `escalation_enabled` / `escalation_interval_minutes` columns in `users`.
- Updated `User` and `EmergencyContact` models with mass assignment fillable arrays and casts.
- Implemented `PUT /api/settings/escalation` to update user escalation preferences.
- Implemented `POST /api/emergency-contacts/reorder` to bulk-update contact priorities using ordered ID lists.
- Refactored `SendInactivityAlerts` job to implement sequential delayed queueing:
  - If escalation is enabled, alerts the contact at the current index.
  - If a subsequent contact exists, dispatches another instance of itself with a delayed schedule matching `escalation_interval_minutes` minutes.
  - Automatically aborts sequential alerts if the `EmergencyAlert` is resolved via check-in before the next job executes.
- Created robust feature integration tests in `InactivityEscalationTest.php` verifying configuration, bulk reordering, sequential dispatching, and conditional cancellation.
- Integrated escalation configuration switch and numeric delay inputs in Web Dashboard (`SecuritySettings.tsx`, `Dashboard.tsx`).
- Integrated reordering controls (drag-and-drop handles and priority arrow buttons) in Web emergency contacts list (`EmergencyContacts.tsx`).
- Integrated escalation configuration switch and numeric delay inputs in Mobile Settings screen (`settings.tsx`, `settings.ts`).
- Integrated priority arrows and list reordering actions in Mobile emergency contacts list (`contacts.tsx`, `emergencyContacts.ts`).

## [2026-06-18] UX Improvement: Auto-scroll on Focus in Web & Mobile settings
- Implemented automatic viewport scrolling (`window.scrollTo`) when text/time/number inputs are focused in `SecuritySettings.tsx` (Hora Inicio, Hora Fin, and Intervalo de escalado) on mobile web browsers, ensuring the input stays 120px from the top of the viewport.
- Integrated a dynamic responsive layout spacer (`h-[350px] md:hidden`) that automatically extends the bottom of the card only when an input is focused on mobile screens, providing ample scrollable space.
- Added bottom padding `pb-32` to the main element in the Web Dashboard (`Dashboard.tsx`).
- Configured `KeyboardAvoidingView` behavior on Android to `'height'` to ensure the screen resizes properly when the keyboard is open in the mobile app.
- Added an `onFocus` handler with a 350ms delay to all numeric inputs on the mobile settings screen (`settings.tsx`) to scroll the `ScrollView` to the end after the keyboard finishes rising.

## [2026-06-18] FASE 8: Gestión de Círculos y Seguridad Avanzada
- **Backend (Laravel & Base de datos):**
  - Creado e implementado `CircleController` y `GeofenceController` con soporte Swagger/OpenAPI.
  - Creada migración añadiendo `user_id` a la tabla `geofences` para permitir geocercas personalizadas por miembro del círculo.
  - Modificado el modelo `Geofence` para incluir la relación `user()` y el scope de coordenadas PostGIS.
  - Actualizado el Job `ProcessGeofencing` para evaluar geocercas específicas basadas en el `user_id` de forma aislada.
  - Cubierta toda la funcionalidad con tests de integración automatizados en `CircleTest.php` y `GeofenceApiTest.php` (tests pasaron 100% limpios).
- **Frontend Web (Next.js):**
  - Diseñado y desarrollado el componente `CircleMap.tsx` usando React Leaflet para mostrar a todos los miembros y geocercas del círculo en un mapa dinámico con soporte de clic para captura de coordenadas.
  - Rediseñada la pestaña de "Rastreo y Círculos" en `Dashboard.tsx` para gestionar completamente círculos (Crear/Unirse), listar miembros, disolver círculos y realizar CRUD de geocercas con asignación por miembro.
- **Frontend Mobile (Expo React Native):**
  - Desarrollado el módulo "Mis Círculos Familiares" en la pantalla de inicio móvil, con panel de creación, adhesión por código de invitación, copiado al portapapeles y visualización de miembros.
  - Implementado soporte de redirección/deep-linking a Google Maps/Apple Maps mediante intents nativos para abrir las coordenadas del miembro seleccionado.
- **Calidad de Código y Estabilización:**
  - Corregidos errores de linter de React/TypeScript y Next.js (`react-hooks/set-state-in-effect` y tipos `any`) en Next.js.
  - Confirmado tipado estático al 100% en Next.js y React Native (`npx tsc --noEmit` exits con 0).

## [2026-06-18] Crisis Feedback & Emergency Responses
- **Backend (Laravel):**
    - Created database migration for `emergency_responses` and user-level `share_contact_responses` privacy configuration.
    - Added model relations, endpoints (`POST /api/emergency-alerts/{id}/respond` and `PUT /api/settings/privacy`), and covered them with integration tests (`EmergencyAlertTest.php`).
    - Regenerated Swagger API documentation.
- **Web Frontend (Next.js):**
    - Redesigned `/emergencia/[id]` (`EmergencyClientPage.tsx`) to implement an interactive support feedback module: entering name and submitting responses ("Marcar como Leído", "Recibido / Enterado", "Voy en camino").
    - Implemented response visibility panel showing list of responses dynamically, conditional on the user's `share_contact_responses` privacy flag.
    - Added a 10-second polling interval to fetch updated data and responses.
    - Integrated a persistent privacy switch in the Wellbeing security settings tab (`SecuritySettings.tsx`, `Dashboard.tsx`).
- **Mobile Frontend (Expo React Native):**
    - Integrated the privacy settings API call in `settings.ts`.
    - Added the "Compartir respuestas de apoyo" toggle in the native settings view (`settings.tsx`).
- **Stabilization:**
    - Fixed React/Next ESLint errors (unused variables and explicit `any` types in catch blocks).
    - Verified all typechecks pass successfully (`npx tsc --noEmit` exits 0) on both frontends.

## [2026-06-18] Passive Auto-Check-in (Wi-Fi & Pedometer)
- **Backend (Laravel):**
    - Created database migration adding `wifi_checkin_enabled`, `safe_wifi_ssid`, and `sensor_checkin_enabled` to `users` table, and `source` to `check_ins` table.
    - Updated `User` and `CheckIn` models fillable arrays and casts.
    - Implemented API route `PUT /api/settings/automation` to update configurations.
    - Modified `POST /api/check-in` to accept and log check-in sources (`manual`, `wifi`, `movement`, `sms`, `whatsapp`).
    - Added comprehensive test suite `AutomationSettingsTest.php` and fixed test unstable sorting in `CheckInHistoryTest.php`.
    - Regenerated Swagger API documentation.
- **Web Frontend (Next.js):**
    - Integrated "Auto-check-in por Wi-Fi" and "Auto-check-in por Actividad Física" configuration options in `SecuritySettings.tsx` (centralized settings panel).
    - Updated `Dashboard.tsx` to display the check-in verification source (e.g. "Vía Wi-Fi seguro", "Vía Sensor de movimiento", etc.) in the wellbeing history log list.
- **Mobile Frontend (Expo React Native):**
    - Installed `expo-sensors` and `@react-native-community/netinfo` libraries using SDK-compatible versions.
    - Integrated settings layout in the settings screen (`settings.tsx`), allowing saving of safe Wi-Fi SSID and toggling sensor checking.
    - Implemented automatic check-in checks inside `locationTask.ts` background task: uses `NetInfo` to match current SSID to user safe SSID and `Pedometer` to detect steps (> 100 steps in 1h), querying the check-in API silently with a 15-minute rate limit.
    - Updated Home screen (`index.tsx`) wellbeing check-in history card to show check-in sources.
- **Stabilization:**
    - Fixed React Native JSX TSX parsing error with raw greater-than (`>`) character by rewriting the label.
    - Verified 100% clean linter execution on Next.js and 0 TypeScript compilation errors in Expo React Native (`npx tsc --noEmit` exits 0).

## [2026-06-18] Branding Redesign (Núcleos & Zonas Seguras) and UX Improvements (FAQs & Tooltips)
- **Naming Redesign (Núcleos & Zonas Seguras):**
    - Refactored user-facing nomenclature across Web and Mobile to adopt unique identifiers:
      - Replaced "Círculos" (Circles) with "Núcleos" to differentiate Estoy Ok from competitors (such as Life360 and GeoZilla).
      - Replaced "Geocercas" (Geofences) and "Perímetros" with "Zonas Seguras" to use warmer, wellbeing-centric terminology.
    - Updated text copy, buttons, labels, and feedback messages in EmergencyClientPage.tsx, page.tsx, Dashboard.tsx, and index.tsx.
    - Maintained backend database tables and models (circles, geofences) unchanged for structural stability.
- **Interactive FAQs & Contextual Tooltips:**
    - Developed an interactive Accordion FAQ section at the bottom of the Web Landing Page (page.tsx) powered by React state (openFaqIndex) for smooth user guidance.
    - Integrated contextual float Tooltips (using Tailwind CSS group-hover transitions) in SecuritySettings.tsx and Dashboard.tsx to guide web users on settings features.
    - Verified mobile settings screen already adopts contextual description microcopy under switches for tactile usability.
- **Stabilization & Validation:**
    - Resolved raw double/single quote issues in JSX causing ESLint parsing errors in SecuritySettings.tsx.
    - Executed all tests and linters; Next.js frontend linter is clean and 79 Laravel integration tests passed 100%.

## [2026-06-19] Low Battery Alerts for Nucleus Members
- **Database & Models (Laravel):**
    - Added database migration defining `battery_level` (float) and `is_battery_low` (boolean) on `current_locations` table, and `low_battery_alerts_enabled` (boolean) and `last_battery_alert_sent_at` (timestamp) on `users` table.
    - Updated `User` and `CurrentLocation` Eloquent models with fillable and cast arrays.
- **Backend API & Queue Alerting:**
    - Updated `/api/locations/update` endpoint to validate `battery_level` and track transitions into low battery (< 15%).
    - Created `SendBatteryAlertJob` to dispatch Expo Push Notifications to all nucleus members when a transition happens, protected by a 1-hour rate limit.
    - Updated `/api/settings/privacy` endpoint to support saving the new `low_battery_alerts_enabled` privacy toggle.
    - Wrote comprehensive feature tests in `BatteryAlertTest.php` verifying the API, database state, settings toggling, and queue rate-limiting logic.
- **Expo React Native App:**
    - Installed `expo-battery` dependency.
    - Updated background `locationTask.ts` execution to query device battery level, store rate-limiting logs in AsyncStorage, and report it inside `/locations/update`.
    - Added the "Alertas de batería baja" configuration toggle inside the native `settings.tsx` screen.
    - Integrated dynamic battery color status indicators and percentages beside member names on the Home screen (`index.tsx`).
- **Next.js Web Frontend & Landing:**
    - Added the new battery alerts configuration switch to `SecuritySettings.tsx`.
    - Created dynamic SVG battery indicators with relative fill levels and status coloring (green/yellow/red with pulsing warning animations) in the Dashboard member sidebar (`Dashboard.tsx`).
    - Added battery status tags inside Leaflet map markers popups (`CircleMap.tsx`).
    - Integrated low battery alert feature bullet item under "Bienestar Activo (Núcleos & Mapa)" and updated battery/signal loss question in interactive FAQ section on the commercial landing page (`page.tsx`).
    - Resolved TypeScript types compiler warnings and verified successful linter & build executions.

## [2026-06-19] Sensor and Connectivity Status Tracking
- **Database & Models (Laravel):**
    - Created database migration to add `is_tracking_active`, `gps_enabled` and `last_seen_at` to the `current_locations` table.
    - Updated `CurrentLocation` model with casts, fillable attributes, and a dynamic appended attribute `is_offline` (returns true if `last_seen_at` is older than 15 minutes and `is_tracking_active` is true).
    - Created `PUT /api/locations/sensor-status` to let the mobile device report sensor updates without sending GPS coordinates.
    - Wrote tests in `SensorStatusTest.php` verifying endpoint, status updating, and `is_offline` accessor behavior.
- **Expo React Native App:**
    - Updated background task in `locationTask.ts` to report `gps_enabled` (via `Location.hasServicesEnabledAsync()`), `is_tracking_active` (true when running), and correct `recorded_at` (derived from `location.timestamp`).
    - Added offline location queuing using `AsyncStorage` when internet is disconnected (or API fails) and sequential flushing once connection is recovered.
    - Synchronized backend/local tracking status on startup (`index.tsx` `fetchUserData` auto-starts or auto-stops the background task depending on user configuration and permissions).
    - Added immediate `PUT /api/locations/sensor-status` updates in `toggleTracking` when toggling tracking.
    - Integrated status indicators for members (Rastreo apagado, GPS desactivado, Sin señal) and hid the map link button for members with deactivated tracking.
- **Next.js Web Frontend & Landing:**
    - Updated `CircleMap.tsx` and `Dashboard.tsx` TypeScript interfaces.
    - Added dynamic status badges in `Dashboard.tsx` sidebar ("Rastreo Apagado", "GPS Desactivado", "Sin Señal") corresponding to members' sensor states.
    - Decreased marker opacity to `0.5` in `CircleMap.tsx` for members with sensor issues or who are offline, and added explicit status warnings in marker popups.
    - Promoted the sensor and connectivity status tracking feature on the commercial landing page (`page.tsx`) by adding a new feature bullet list item and updating the signal loss answer in the interactive FAQs.
    - Fixed a React hydration error in the dashboard's invite code section by changing the outer `<p>` tag wrapping nested `<div>`s to a `<div>` tag.
- **Stabilization & Validation:**
    - Verified all typechecks pass successfully (`npx tsc --noEmit` exits 0) on both frontends (web and mobile).
    - Verified backend integration test suite runs 100% successfully (87 passes).

## [2026-06-19] Tracking History Map Centering & Markers Fix
- **Expo React Native App:**
    - Introduced a `mapReady` state in `history.tsx` and bound it to `<MapView onMapReady={() => setMapReady(true)} />`.
    - Configured `<MapView>` to use `provider={PROVIDER_GOOGLE}` on iOS to match Android and the external Google Maps layout, providing a unified and consistent design system.
    - Added navigation parameter `mode` (`'live'` from "Ver" and `'history'` from "Ruta") to dynamically switch the map screen's top header title text ("Ubicación de <Miembro>" vs "Ruta de <Miembro>").
    - Added a floating header button labeled "Cómo llegar" (with Map icon) to let the user optionally launch the external Google Maps app for direct GPS navigation to the active location.
    - Created a reactive `useEffect` listening to `[points, mapReady, memberInfo]` to trigger map camera animations (`animateToRegion`) only when the native map layer is fully layout-ready.
    - Modified `fetchHistory` to map and explicitly parse coordinate response fields (`latitude` and `longitude`) to float `Number` values.
    - Added dynamic unique React `key` props (incorporating active indexes and IDs) to map markers to force React Native Maps to re-create and correctly position markers upon state/coordinate updates.
    - Added a fallback marker (`chinche`) showing the member's last known location and timestamp on the map when no history coordinates exist for the selected date.
    - Added a background polling interval (every 10 seconds) on the home screen (`index.tsx`) to silently fetch circles (`fetchCircles(true)`) and user data to dynamically update members' active locations, battery percentages, and sensor tracking states in real-time.
- **Next.js Web Frontend & Landing:**
    - Added **Historial y Reproducción de Rutas** under the "Bienestar Activo (Círculos/Núcleos & Mapa)" section on the commercial landing page (`page.tsx`).
    - Corrected pricing table copy mismatch (changed free plan history limit from 48 hours to the actual 24 hours constraint, and highlighted route playback support on the Premium plan features list).
    - Verified TypeScript type safety compiles without errors (0 errors) on both frontends (web and mobile).

## [2026-06-21] SOS Silencioso con Ubicación en Alta Frecuencia y Grabación Ambiental
- **Backend (Laravel):**
    - Creada migración para agregar el campo `audio_path` a la tabla `emergency_alerts`.
    - Actualizado el modelo `EmergencyAlert` con accessor dinámico `audio_url` y propiedad fillable.
    - Creado endpoint `POST /api/emergency-alerts/sos` para disparar SOS Silencioso, notificar por Push prioritario a miembros del núcleo y despachar SMS de urgencia vía Twilio.
    - Creado endpoint `POST /api/emergency-alerts/{id}/audio` para subir grabaciones ambientales multipart/form-data.
    - Agregada documentación OpenAPI/Swagger para los nuevos endpoints.
    - Escrita suite completa de tests automatizados (15 tests exitosos en total).
- **Web Frontend (Next.js):**
    - Rediseñada cabecera de `EmergencyClientPage.tsx` con animación pulsante roja para alertas de tipo `silent_sos`.
    - Integrado un reproductor de audio ambiental condicional si la alerta posee una grabación de audio disponible (`audio_url`).
    - Modificado el intervalo de polling de ubicación en vivo en la pantalla de crisis a cada 5 segundos si el SOS está activo.
    - Añadidas insignias visuales parpadeantes y botón "Ver Alerta" en el sidebar del Dashboard para destacar miembros con SOS activo.
    - Soportado el renderizado en tiempo real de SOS y el reproductor de audio dentro del popup del marcador en el mapa.
- **Mobile Frontend (Expo / React Native):**
    - Instalada la librería `expo-av` para gestión de audio y grabación sin fricción.
    - Creado servicio de API `sosService.ts` para manejar peticiones de inicio de SOS y subida de audio.
    - Implementada lógica de SOS en `index.tsx` que inicia un rastreo GPS de máxima frecuencia (5s) y graba 15s de audio en segundo plano al pulsar el botón SOS.
    - Agregado canal de soporte offline mediante `Linking` para disparar un SMS nativo a los contactos de emergencia si falla la red.
    - Diseñado botón SOS prominente en el Dashboard y estilos correspondientes, logrando un 100% de type safety en TypeScript.

## [2026-06-22] S.O.S. Mobile Layout Styling and State Sync Correction & Landing Page Promotion
- **Mobile Frontend (Expo / React Native):**
    - Removed nested `<View>` badge from the circular member name list inside `index.tsx` and replaced it with a custom-styled `<Text style={styles.sosTextInline}>` badge to avoid layout calculations overflow and wrapping glitches.
    - Modified `memberActions` display inside the circles member list to hide redundant tracking buttons ("Ver", "Ruta") and crisis buttons ("Alerta") for the current logged-in user (`isSelf === true`). This ensures the user's card remains clean, fitting perfectly on smaller mobile displays.
    - Modified S.O.S. activation flow inside `handleSilentSos` to trigger `await fetchCircles(true)` immediately after successful API activation, allowing the member list to reflect the active S.O.S. status without waiting for the 10-second polling ticks.
    - Synchronized `isSosActive` local state reactively via a new `useEffect` that listens to `circles` changes and maps whether the user's member object has active S.O.S. alerts. This allows S.O.S. states to sync properly across mounts, logins, and remote updates.
    - Aligned the `borderWidth` parameter to a constant `1.5` on both `memberItem` and `memberItemSosActive` stylesheet definitions, ensuring that toggling border colors does not trigger layout recalculation bugs on native layout engines.
    - Verified compile success with `npx tsc --noEmit` and run success.
- **Web Frontend (Next.js):**
    - Added **S.O.S. de Emergencia (Crisis Activa)** as the third core security pillar inside `page.tsx` features grid section, detailing the silent trigger, high-frequency (5s) GPS updates, environmental audio recording, and SMS backup features.
    - Cleaned up naming copy in the premium features list from "Zonas Seguras (Zonas Seguras)" to "Zonas Seguras" and added "S.O.S. Silencioso con Grabación de Audio y Rastro Crítico (5s)" as a key PRO benefit.
    - Added a detailed S.O.S. question and answer inside the interactive Accordion FAQ section, explaining the trigger process, background recording, and notifications.
    - Ran Next.js Turbopack compiler build successfully (0 compile warnings or errors).

## [2026-06-22] Detección Automática de Trayectos y Excesos de Velocidad Vehicular
- **Backend (Laravel & Base de Datos):**
    - Creada tabla `drive_events` con test de integración y columnas `speed_limit` en la tabla `circles`, y `speed` / `is_driving` en `current_locations` y `location_histories`.
    - Actualizado el endpoint `POST /api/locations/update` para validar velocidad, convertirla de m/s a km/h, abrir y cerrar trayectos activos de conducción, y gatillar alertas push a través del Job `SendSpeedingAlertJob` con rate-limiting mediante caché.
    - Añadida ruta `PUT /api/circles/{circle}/speed-limit` para actualizar el límite de velocidad por círculo.
    - Creado comando `drive:cleanup-active` en el Scheduler para cerrar trayectos huérfanos que no actualizan hace más de 30 min.
    - Desarrollada suite completa de pruebas unitarias y de integración en `DriveDetectionTest.php` (tests pasaron 100% limpios).
- **Mobile Frontend (Expo / React Native):**
    - Implementada lógica de detección de velocidad con histéresis (1 min sostenido sobre 25 km/h para iniciar `is_driving = true`, y 2 min de tolerancia para finalizar) guardando estados en AsyncStorage en `locationTask.ts`.
    - Añadido el badge *"🚗 [Velocidad] km/h"* inline al listado de miembros del núcleo de la Home Screen.
    - Agregado el endpoint `updateCircleSpeedLimit` al servicio de configuración y el formulario visual para que el dueño ajuste el límite de velocidad en la pantalla de Ajustes.
    - Corregido error de importación de `react-native-maps` en la compilación web de Metro extrayendo el mapa de historial a un componente con extensiones de plataforma (`HistoryMap.tsx` y `HistoryMap.web.tsx`).
    - Verificado tipo de tipado y compilación exitosa (`npx tsc --noEmit` exits con 0).
- **Web Frontend (Next.js):**
    - Actualizado `CircleMap.tsx` para soportar marcadores animados e interactivos de vehículos (`🚗`) en Leaflet con colores dinámicos (rojo pulsante para excesos de velocidad) y badges del velocímetro en popups.
    - Añadida la insignia de velocidad vehicular *"🚗 [Velocidad] km/h"* inline al listado lateral de miembros y agregada la sección de edición del límite de velocidad en el sidebar de configuración del círculo activo.
    - Promocionada la característica en la Landing Page comercial (`page.tsx`) agregando el bullet de velocidad vehicular en características, beneficio en el plan PRO y pregunta detallada en el acordeón de FAQs.
  ## [2026-06-22] Detección Automática de Accidentes Vehiculares mediante Acelerómetro
- **Backend (Laravel & Base de Datos):**
    - Creada tabla `crash_events` y modelo `CrashEvent` con relaciones y casts de datos espaciales.
    - Creado endpoint `POST /api/alerts/crash` para registrar impactos de colisión, iniciar la alerta de emergencia y despachar de forma síncrona/inmediata el Job `SendCrashAlertJob`.
    - Creado endpoint `POST /api/alerts/crash/{id}/false-alarm` para que el usuario pueda desactivar la alerta en cualquier momento indicando que fue una falsa alarma, lo cual actualiza el estado del impacto y resuelve la alerta en base de datos.
    - Creado Job síncrono `SendCrashAlertJob` para enviar notificaciones Push (Expo) de alta prioridad con sonido crítico a los miembros del núcleo y alertas SMS/WhatsApp (Twilio) de forma inmediata a los contactos de emergencia.
    - Actualizado el endpoint de consulta de alertas `GET /api/emergency-alerts/{id}` para inyectar detalles del impacto (`crash_info`) si el tipo de la alerta es `crash`.
    - Desarrollada suite completa de pruebas de integración en `CrashAlertTest.php` y verificada la cobertura libre de regresiones (106 tests pasados con éxito).
    - Regenerada documentación de la API en OpenAPI/Swagger.
- **Mobile Frontend (Expo / React Native):**
    - Implementada lógica de suscripción al sensor `Accelerometer` en segundo plano en `locationTask.ts` únicamente cuando el estado de conducción (`is_driving`) está activo, optimizando la batería.
    - Añadido algoritmo de análisis de aceleración vectorial $a_{neta} = \sqrt{x^2+y^2+z^2} \ge 4.5$G seguido de 3 segundos de inmovilidad total (varianza respecto a 1G $< 0.15$G) para detectar patrones físicos reales de colisión.
    - Desarrollado el componente global `CrashPreAlertOverlay.tsx` para mostrar una pantalla crítica de cuenta regresiva de 15 segundos con reproducción de sirena en bucle (`expo-av`), permitiendo al usuario cancelar la pre-alerta pulsando "Estoy bien".
    - Configurado el disparo automático del protocolo de emergencia máxima si la cuenta regresiva expira (petición a la API de colisión, inicio de tracking GPS crítico cada 5s, grabación de audio ambiental SOS y fallback de SMS local nativo).
    - Adaptada la Home Screen móvil (`index.tsx`) para soportar la visualización del estado de emergencia de colisión y resolver/cancelar la alerta directamente como falsa alarma.
- **Web Frontend (Next.js):**
    - Rediseñada la visualización pública de crisis `/emergencia/[id]` (`EmergencyClientPage.tsx`) con un tema rojo parpadeante de colisión y una grilla dedicada con los detalles del impacto (Fuerza G, Velocidad de impacto, y Estado del vehículo).
    - Actualizado el mapa de Leaflet (`EmergencyMap.tsx`) para renderizar un marcador interactivo de vehículo (`🚗`) con una animación pulsante de ondas de choque de Leaflet, velocímetro a 0 km/h y una ventana emergente detallada indicando la fuerza G.
    - Configurado el polling de ubicación acelerado a 5 segundos si el tipo de alerta activa es `crash`.

## [2026-06-22] Geocercas Móviles y Alertas de Proximidad Relativas
- **Backend (Laravel & Base de Datos):**
    - Creada migración para la tabla `dynamic_geofences` y el campo `proximity_alerts_enabled` en la tabla `users` (ya ejecutada).
    - Creados modelo `DynamicGeofence` y Job asíncrono `ProcessDynamicGeofencing` que calcula la distancia geodésica exacta en metros mediante PostGIS (`ST_Distance` con cast a `geography`).
    - Implementado rate-limiting de 15 minutos en caché para prevenir spam de notificaciones, y notificaciones de tranquilidad al reingresar al radio seguro.
    - Desarrollados endpoints `GET /api/dynamic-geofences/active`, `POST /api/dynamic-geofences`, `POST /api/dynamic-geofences/{id}/deactivate` y `PUT /api/settings/proximity-alerts`.
    - Integrada respuesta con flag `active_dynamic_geofence: true/false` en el controlador de actualización de ubicación (`LocationController`).
    - Desarrollada suite completa de pruebas en `DynamicGeofenceTest.php` (tests pasaron 100% exitosamente).
    - Regenerada la especificación de OpenAPI/Swagger.
- **Mobile Frontend (Expo / React Native):**
    - Implementada lógica en `locationTask.ts` para cambiar dinámicamente el tracking de GPS de fondo a alta frecuencia (5s) si `active_dynamic_geofence` es verdadero.
    - Añadidos listeners para notificaciones push en primer plano para activar vibración persistente nativa (`Vibration.vibrate`) y alert dialog con botón para detenerla.
    - Agregado switch de privacidad en la pantalla de ajustes (`settings.tsx`).
    - Agregados botones de activación radar y modales de selección de radio (30m, 50m, 100m) en el listado de miembros del núcleo de la Home Screen (`index.tsx`).
- **Web Frontend (Next.js):**
    - Agregado el switch de configuración de privacidad "Alertas de proximidad relativas" en `SecuritySettings.tsx` con sincronización e interfaces tipadas en `Dashboard.tsx`.
    - Actualizado `CircleMap.tsx` de Leaflet para renderizar un círculo punteado sobre el tutor, y una línea conectora punteada entre miembros con tooltip flotante interactivo de distancia en vivo que cambia a color rojo en alerta de exceso de rango.
    - Compilación de producción Next.js probada y finalizada con éxito (0 advertencias o errores).

## [2026-06-25] Issue 1: Planes y Pasarelas de Pago en la Web
- **Web Frontend (Next.js):**
    - Creado el componente [BillingSection.tsx](file:///home/usuario/aplicaciones/estoyok/frontend-web/src/components/BillingSection.tsx) para comparar características de planes (Gratis vs Premium PRO) y gestionar el selector de pasarelas de pago (Stripe, Mercado Pago y PayPal).
    - Integrado el flujo de redireccionamiento asíncrono para Mercado Pago y PayPal llamando a `POST /api/subscriptions/checkout`.
    - Agregado el tab `Planes y Membresía` en la barra lateral del [Dashboard.tsx](file:///home/usuario/aplicaciones/estoyok/frontend-web/src/components/Dashboard.tsx) y actualizado el banner lateral "Mejorar a PRO" para abrir la interfaz de facturación directamente.
    - Soportada la visualización del estado Premium Activo con badges interactivos para usuarios suscritos.
    - Validado al 100% libre de errores con `npm run lint` y pruebas unitarias de backend.

## [2026-06-25] Optimización de Conversión de Membresías (Planes de Pago y Copy)
- **Web Frontend (Next.js):**
    - Rediseñado por completo el copy de la propuesta de valor y los beneficios del plan Premium PRO en el [BillingSection.tsx](file:///home/usuario/aplicaciones/estoyok/frontend-web/src/components/BillingSection.tsx) y en la Landing Page comercial ([page.tsx](file:///home/usuario/aplicaciones/estoyok/frontend-web/src/app/page.tsx)), destacando los tres pilares de seguridad familiar.
    - Implementado un simulador interactivo de notificaciones de WhatsApp y SMS en [BillingSection.tsx](file:///home/usuario/aplicaciones/estoyok/frontend-web/src/components/BillingSection.tsx) que renderiza un mockup de celular en tiempo real, permitiendo a los usuarios ver los mensajes reales de emergencia (Inactividad, SOS, Accidentes).
    - Verificado con `npm run lint` logrando un 100% de éxito libre de advertencias y ejecución exitosa de los 114 tests del backend de Laravel.

## [2026-06-25] Issue 2: Checkout Seguro con Stripe Elements
- **Web Frontend (Next.js):**
    - Instaladas las dependencias `@stripe/stripe-js` y `@stripe/react-stripe-js` en el contenedor `frontend`.
    - Inicializado el cargador seguro con la clave pública publishable en [BillingSection.tsx](file:///home/usuario/aplicaciones/estoyok/frontend-web/src/components/BillingSection.tsx).
    - Creado el subcomponente seguro `StripeCheckoutForm` implementando elementos divididos (`CardNumberElement`, `CardExpiryElement` y `CardCvcElement`) con estilos integrados para coincidir con la estética oscura de Estoy Ok.
    - Implementadas validaciones de errores en tiempo real y detector dinámico de logotipo de marca (Visa, Mastercard, Amex, Discover, Diners, JCB, UnionPay) según los inputs del usuario.
    - Implementada lógica de tokenización interactiva (`stripe.createPaymentMethod`) que genera de forma segura el `payment_method_id` sin tocar los datos de tarjeta directamente (PCI-DSS compliant), mostrando un spinner y alertando de su éxito de cara a su posterior envío a Laravel en el Issue 4.
    - Resueltos los warnings de compilación de TypeScript para asegurar consistencia de tipos.

## [2026-06-25] Issue 3: Integración del SDK Móvil de Stripe (Expo / React Native)
- **Mobile Frontend (Expo / React Native):**
    - Configurado el proveedor global `<StripeProvider>` en el layout raíz móvil `_layout.tsx`.
    - Refactorizado el flujo de suscripción en la pantalla de Ajustes `settings.tsx` para utilizar la interfaz segura e integrada de Stripe **PaymentSheet** (`useStripe` hooks) en lugar de los inputs inline antiguos de `<CardField>`.
    - Desarrollado un simulador nativo de alta fidelidad `StripeMockPaymentSheetModal` que imita con precisión el comportamiento del PaymentSheet oficial de Stripe en entornos como **Expo Go** o la compilación web.
    - Implementadas validaciones nativas de Luhn, formato de expiración automática MM/YY, longitud de CVC dinámica por marca (Visa, Mastercard, Amex, Discover) y detección visual interactiva de marca de tarjeta.
    - Integrado soporte nativo para flujos de autenticación bancaria **3D Secure / SCA** simulada utilizando la tarjeta de prueba de Stripe (`4000002760003184`), disparando flujos de autorización interactiva.
    - Integrado el punto de conexión con la API de Laravel para enviar el `payment_method_id` resultante y simular la activación de Premium PRO del usuario en local de forma interactiva en la app.
    - Ejecutado chequeo de compilación de TypeScript móvil con éxito total (`npx tsc --noEmit` exits with 0).

## [2026-06-25] Mobile Layout Reorganization & Bottom Tab Navigation
- **Mobile Frontend (Expo / React Native):**
    - Redesigned and redistributed the mobile interface from a single scrollable dashboard screen into 4 distinct bottom navigation tabs:
        1. `🏠 Panel`: Dedicated wellbeing check, daily check-in indicator, history of reports, and S.O.S emergency button.
        2. `📍 Mapa`: User GPS tracking toggles, relative proximity radar alerts switches, active circles selection lists, and nucleus member locations/sensor monitoring (battery, speedometer, signal loss status) with routing map options.
        3. `👥 Familia`: Circle admin capabilities including invitation code copying, joining/creating nuclei, and administrative member management (leave/remove circles).
        4. `⚙️ Ajustes`: Profile summary, shortcuts navigating to `/contacts` and `/settings` stacks, and user logout button.
    - Implemented a custom state-based bottom navigation tab bar in `index.tsx` (Option A) to preserve background tracking tasks, accelerometer/driving hysteresis context, and active websocket/polling intervals without unmounting.
    - Handled screen safe areas and styles using high-quality aesthetics consistent with modern dark mode elements.
    - Verified compilation success with `npx tsc --noEmit` and ran the Laravel backend integration test suite with 100% success rate (114/114 passing).

## [2026-06-25] Mobile Layout Clean-up: Premium Decoupling
- **Mobile Frontend (Expo / React Native):**
    - Cleaned up the "Configuración de Seguridad" screen (`settings.tsx`) by removing all references, variables, helper validation functions, event handlers, and simulation modal for Stripe payments.
    - Simplified imports, removing `@stripe/stripe-react-native`, `AsyncStorage`, `api`, and unused Lucide icons from `settings.tsx`.
    - Resolved TypeScript compiling errors by ensuring the newly introduced `modalHeader` layout properties are defined inside the StyleSheet structure of the main `index.tsx` screen.
    - Verified compilation safety with `npx tsc --noEmit` and confirmed zero regressions in the Laravel backend test suite (114/114 passing).

## [2026-06-28] Estabilización de Túnel ngrok y Depuración de Errores de Red
- **Configuración y Guía de ngrok:**
    - Creado el documento [pruebas_con_tunel_ngrok.md](file:///home/usuario/aplicaciones/estoyok/docs/pruebas_con_tunel_ngrok.md) para registrar el proceso detallado del uso de túneles ngrok personales con dominio estático fijo para pruebas físicas fuera del Wi-Fi en Expo Go, evadiendo el error de saturación de la cuenta de Expo (`ERR_NGROK_108`).
- **Mobile Frontend (Expo / React Native):**
    - Implementada comprobación de sesión activa (`auth_token` de AsyncStorage) al inicio de la tarea en segundo plano en [locationTask.ts](file:///home/usuario/aplicaciones/estoyok/mobile/src/services/locationTask.ts). Si el usuario está deslogueado, el proceso finaliza inmediatamente para evitar llamadas de red no autorizadas y spamear errores `401`.
    - Modificado el comportamiento de logs de error en `locationTask.ts`, rebajando `console.error` de red/sincronización a `console.log`. Esto previene que se dispare el overlay de pantalla roja (redbox) en Expo Go ante microcortes o transiciones de señal móvil.
- **Backend & Despliegue (Railway):**
    - Diagnosticado y corregido el error de enlace 404 en el mensaje de emergencia SOS por WhatsApp. Se actualizó la variable de entorno `FRONTEND_URL` en Railway para apuntar al subdominio del frontend activo real (`https://frontend-web-production-f4f0.up.railway.app`), permitiendo la correcta visualización de la posición del usuario en el mapa de crisis.
    - Ejecutada la suite de pruebas del backend de Laravel con éxito (114/114 tests pasados).
- **Mobile Frontend (Expo / React Native):**
    - Corregido crash `TypeError: activePoint.accuracy.toFixed is not a function` en [history.tsx](file:///home/usuario/aplicaciones/estoyok/mobile/src/app/history.tsx) asegurando que la precisión (`accuracy`) obtenida de la API del historial se castee a tipo numérico con `Number()` al procesar los puntos y al formatear su visualización.
    - Agregado el indicador "Visto por última vez: HH:MM" en la lista de familiares del index.tsx de la aplicación.
    - Verificación exitosa de compilación de Typescript (`npx tsc --noEmit` exits with 0).
- **Web Frontend (Next.js):**
    - Agregado el indicador "Visto por última vez: HH:MM" en la barra lateral de miembros del Dashboard.tsx para facilitar el monitoreo directo de actualizaciones.
    - Corregido error de compilación TypeScript (`Property 'recorded_at' does not exist on type...`) al agregar la propiedad opcional `recorded_at` al tipo `current_location` en [Dashboard.tsx](file:///home/usuario/aplicaciones/estoyok/frontend-web/src/components/Dashboard.tsx) y [CircleMap.tsx](file:///home/usuario/aplicaciones/estoyok/frontend-web/src/components/CircleMap.tsx).
    - Compilación de producción Next.js probada y finalizada con éxito localmente en el contenedor docker (`npm run build` exit code 0).

## [2026-07-06] Migración Kotlin: Etapa 1 Completada
- Inicializado el proyecto nativo en el directorio `android-native/` usando Gradle Kotlin DSL (`settings.gradle.kts`, root `build.gradle.kts`, app `build.gradle.kts`).
- Configurado el soporte para Dagger Hilt, Retrofit, Room, DataStore, Google Maps y Stripe SDK.
- Configurado el archivo de manifiesto `AndroidManifest.xml` con todos los permisos requeridos de localización en segundo plano, sensores, audio, notificaciones y SMS.
- Implementado el tema visual oscuro premium (`Theme.kt`, `Color.kt`, `Type.kt`) reflejando la identidad de Estoy Ok.
- Creado `SessionManager.kt` utilizando Android DataStore Preferences para la gestión reactiva del token de autenticación.
- Implementado `AuthInterceptor.kt` y `NetworkModule.kt` en Retrofit para inyectar automáticamente el Bearer Token en las llamadas de red.
- Implementada la barra de navegación inferior (Bottom Navigation) usando Jetpack Compose Navigation conectando las cuatro pantallas principales: [PanelScreen.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/wellbeing/presentation/PanelScreen.kt), [MapaScreen.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/presentation/MapaScreen.kt), [FamiliaScreen.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/presentation/FamiliaScreen.kt) y [AjustesScreen.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/wellbeing/presentation/AjustesScreen.kt) mediante `NavGraph.kt`.

## [2026-07-06] Migración Kotlin: Etapa 2 Completada
- Creados los DTOs de autenticación (`AuthModels.kt`) para peticiones y respuestas serializables del backend.
- Creado e integrado el servicio de red [AuthApiService.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/auth/data/remote/AuthApiService.kt).
- Desarrollado [AuthRepositoryImpl.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/auth/data/repository/AuthRepositoryImpl.kt) con lógica de persistencia de sesión a través de DataStore, y manejador/parser de errores de validación de Laravel (ej. estructura de errores anidados).
- Vinculadas las dependencias de repositorio y red en el módulo Hilt [AuthModule.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/auth/di/AuthModule.kt).
- Creados los ViewModels de Compose state-holders: [AuthViewModel.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/auth/presentation/AuthViewModel.kt) (estado reactivo de sesión para la app), [LoginViewModel.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/auth/presentation/login/LoginViewModel.kt), [RegisterViewModel.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/auth/presentation/register/RegisterViewModel.kt) y [VerifyEmailViewModel.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/auth/presentation/verify/VerifyEmailViewModel.kt).
- Desarrolladas las vistas completas utilizando Jetpack Compose:
  - [LoginScreen.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/auth/presentation/login/LoginScreen.kt): Formulario con validación en tiempo real de email, campo de contraseña con toggle visual de visibilidad (icono de ojo) y botón de acceso.
  - [RegisterScreen.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/auth/presentation/register/RegisterScreen.kt): Inputs para nombre, email, contraseña con validación de coincidencia y longitud, teléfono con soporte de prefijo `+` y texto informativo explicativo de formato E.164.
  - [VerifyEmailScreen.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/auth/presentation/verify/VerifyEmailScreen.kt): Campo numérico centrado para código OTP de 6 dígitos con visualización espaciada, manejo de reenvío de código e indicadores correspondientes de carga/éxito.
- Integradas las pantallas en el flujo de navegación condicional de [NavGraph.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/core/navigation/NavGraph.kt), manejando la redirección automática basada en el estado `isAuthenticated`.

## [2026-07-06] Migración Kotlin: Etapa 3 Completada
- Creados los DTOs de bienestar y configuraciones: [CheckInModels.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/wellbeing/data/model/CheckInModels.kt) (historial y registro), [ContactModels.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/wellbeing/data/model/ContactModels.kt) (contactos de emergencia y solicitudes de prioridad) y [SettingsModels.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/wellbeing/data/model/SettingsModels.kt) (intervalo de reportes, Modo Sueño, Twilio y automatizaciones pasivas).
- Desarrollados los clientes de API de Retrofit: `CheckInApiService.kt`, `EmergencyContactsApiService.kt` y `SettingsApiService.kt`.
- Implementados y vinculados por Dagger Hilt los repositorios: `CheckInRepositoryImpl.kt`, `EmergencyContactsRepositoryImpl.kt` y `SettingsRepositoryImpl.kt` para coordinar de forma aislada las transacciones de red de bienestar.
- Creado e integrado [PanelViewModel.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/wellbeing/presentation/PanelViewModel.kt) para gestionar las operaciones de check-in manual, refresco dinámico de datos y cálculo adaptativo del estado de inactividad del usuario (Modo Debug trata el intervalo como minutos; Producción como horas).
- Creado e integrado [AjustesViewModel.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/wellbeing/presentation/AjustesViewModel.kt) para gestionar el CRUD y reordenación de contactos por prioridad, la activación del Modo Sueño con zona horaria del dispositivo, y el toggle de auto-check-ins por SSID de Wi-Fi o movimiento.
- Reconstruidas las vistas de UI en Jetpack Compose:
  - [PanelScreen.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/wellbeing/presentation/PanelScreen.kt): Posee la cabecera dinámica de usuario, la tarjeta adaptativa de estado ("Protegido y a Salvo", "Reporte Vencido", "Sin Reportes") con colores temáticos de seguridad, el botón circular flotante "Estoy OK" con spinner e indicador de envío, y la sección de historial de reportes con badges indicando el origen (manual, wifi, sensor, etc.).
  - [AjustesScreen.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/wellbeing/presentation/AjustesScreen.kt): Posee la tarjeta de perfil, chips de intervalo de reporte (12h, 24h, 48h), Modo Sueño con selectores y switches, Toggle de SMS/WhatsApp, Auto-check-in pasivo (SSID y acelerómetro), y el CRUD completo de contactos de emergencia con flechas arriba/abajo de reordenación secuencial de prioridades y validador de formato E.164.

## [2026-07-06] Migración Kotlin: Etapa 4 Completada
- Creados los DTOs de ubicación y círculos: [LocationModels.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/data/model/LocationModels.kt) y [CircleModels.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/data/model/CircleModels.kt).
- Desarrollados los clientes de API de Retrofit: `LocationApiService.kt` y `CircleApiService.kt`.
- Implementada la base de datos Room para la cola de ubicación offline: [OfflineLocationEntity.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/data/local/entity/OfflineLocationEntity.kt), [OfflineLocationDao.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/data/local/dao/OfflineLocationDao.kt) y [EstoyOkDatabase.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/core/data/local/EstoyOkDatabase.kt) configurada mediante la inyección del módulo Hilt [DatabaseModule.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/core/di/DatabaseModule.kt).
- Desarrollado [LocationRepositoryImpl.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/data/repository/LocationRepositoryImpl.kt) con lógica de fallback offline en Room ante fallos de conexión a internet o errores de servidor (PCI-DSS compliant y libre de pérdida de coordenadas), e implementada la lógica de vaciado/sincronización ordenada de la cola.
- Creado [LocationSyncWorker.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/services/LocationSyncWorker.kt) (WorkManager) para disparar el vaciado de cola automáticamente al recuperar la conexión.
- Actualizado [EstoyOkApplication.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/EstoyOkApplication.kt) para proveer configuración personalizada de WorkManager con Hilt.
- Desarrollado [TrackingService.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/services/TrackingService.kt): Un Android Foreground Service que captura la posición GPS en segundo plano usando `FusedLocationProviderClient`, lee el estado de la batería/red y reporta la telemetría a la API de Laravel con lógica de aceleración de frecuencia si se detecta radar de proximidad activo.
- Creado e integrado [MapaViewModel.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/presentation/MapaViewModel.kt) para gestionar el polling periódico (cada 10s) de miembros del núcleo activo, controlar los dropdowns de selección y disparar/detener el servicio persistente de fondo mediante Intents.
- Reconstruida la vista nativa de mapa en Jetpack Compose [MapaScreen.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/presentation/MapaScreen.kt) integrando:
  - Google Map con marcadores reactivos para cada miembro del núcleo.
  - Switch interactivo para iniciar/detener el servicio persistente de fondo.
  - Carrusel inferior de tarjetas de familiares detallando telemetría (batería, velocidad, signal-loss) y badges de sensores (GPS desactivado, Sin Señal, Conduciendo).
  - Botón interactivo de redirección directa a la aplicación nativa Google Maps para navegación GPS guiada.

## [2026-07-06] Migración Kotlin: Etapa 5 Completada
- Creados los DTOs de detección de colisiones y falsas alarmas: [CrashModels.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/data/model/CrashModels.kt).
- Creado e integrado el cliente de API de Retrofit [CrashApiService.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/data/remote/CrashApiService.kt).
- Desarrollado [CrashRepositoryImpl.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/data/repository/CrashRepositoryImpl.kt) con soporte para reportar impactos a la API (`POST /alerts/crash`) y gestionar falsas alarmas (`POST /alerts/crash/{id}/false-alarm`).
- Vinculado en el módulo Hilt [TrackingModule.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/di/TrackingModule.kt).
- Actualizado [TrackingService.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/services/TrackingService.kt) para:
  - Implementar la interfaz `SensorEventListener` y escuchar al acelerómetro (`Sensor.TYPE_ACCELEROMETER`).
  - Implementar el algoritmo de histéresis de conducción (velocidad > 25 km/h por 1 minuto activa conducción; velocidad <= 25 km/h por 2 minutos desactiva conducción - reducidos a 5s/10s en modo debug para simplificar pruebas locales).
  - Activar el acelerómetro únicamente cuando `isDriving` sea verdadero para optimizar el consumo de batería.
  - Implementar la lógica matemática vectorial de colisiones: aceleración neta mayor o igual a $4.5$G, seguida por una ventana post-impacto de 3 segundos con análisis de varianza menor a $0.15$G para verificar inmovilidad del dispositivo.
  - Lanzar de forma segura el intent para abrir la actividad de pre-alerta crítica.
- Desarrollada la pantalla de alerta a pantalla completa [CrashAlertActivity.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/presentation/CrashAlertActivity.kt):
  - Diseñada con interfaz oscura de alta fidelidad y alerta de colisión de color rojo de alto impacto.
  - Reproduce en bucle el tono de alarma nativo del dispositivo (`RingtoneManager.TYPE_ALARM`) y bloquea el botón de retroceso (`onBackPressed`) para evitar bypass accidental.
  - Maneja un temporizador visual descendente de 15 segundos.
  - Botón destacado "ESTOY BIEN - CANCELAR" que detiene la sirena y finaliza el proceso sin reportar.
  - Lógica de vencimiento de tiempo: realiza la petición API de accidente de forma asíncrona, acelera el rastreo GPS en segundo plano a máxima frecuencia (5s) y activa el canal de respaldo local SMS enviando un mensaje directo a cada contacto de emergencia con las coordenadas de la colisión.
- Registrada la nueva actividad en el manifiesto `AndroidManifest.xml`.

## [2026-07-06] Migración Kotlin: Etapa 6 Completada
- Creados los DTOs de inicio de alerta silenciosa SOS y subida de clips de voz: [SosModels.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/data/model/SosModels.kt).
- Desarrollados los clientes de API de Retrofit [SosApiService.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/data/remote/SosApiService.kt) incorporando mapeos para subida multipart/form-data.
- Desarrollado [SosRepositoryImpl.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/data/repository/SosRepositoryImpl.kt) con lógica de envío de alarmas SOS y subida asíncrona de archivos.
- Habilitada la inyección de dependencias en Hilt en [TrackingModule.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/di/TrackingModule.kt).
- Creado [AudioRecorder.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/core/util/AudioRecorder.kt) encapsulando el uso de `MediaRecorder` de Android (MPEG-4/AAC) para grabaciones de sonido ambiental en segundo plano hacia la caché de la aplicación.
- Actualizado [PanelViewModel.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/wellbeing/presentation/PanelViewModel.kt) para incorporar lógica SOS:
  - Lógica asíncrona de disparo API de SOS (`POST /emergency-alerts/sos`).
  - Lógica de grabación de audio ambiente silencioso durante 15 segundos y posterior subida multipart al backend.
  - Lógica de fallback local: si hay error de red al iniciar la alarma, se dispara un envío local de SMS por `SmsManager` a todos los familiares del usuario.
- Actualizado [PanelScreen.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/wellbeing/presentation/PanelScreen.kt) incorporando un botón SOS rojo de emergencia en la barra de cabecera:
  - Implementado mediante `pointerInput` y `detectTapGestures`.
  - El click simple gatilla un Toast educativo ("Mantén presionado por 3 segundos para activar SOS").
  - La pulsación larga (3 segundos) activa de forma segura el SOS silencioso, previniendo falsos disparos.

## [2026-07-06] Migración Kotlin: Etapa 7 Completada
- Creados los DTOs de checkout de Stripe/MercadoPago/PayPal: [SubscriptionModels.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/data/model/SubscriptionModels.kt).
- Creada e integrada la interfaz de red [SubscriptionApiService.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/data/remote/SubscriptionApiService.kt) para facturación Premium.
- Implementado el repositorio de subscripción [SubscriptionRepositoryImpl.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/data/repository/SubscriptionRepositoryImpl.kt) e inyectado con Hilt en [TrackingModule.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/di/TrackingModule.kt).
- Desarrollado [FamiliaViewModel.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/presentation/FamiliaViewModel.kt) para coordinar las operaciones CRUD locales y de red de círculos, así como la consulta de pasarelas de pago de Stripe.
- Diseñada e implementada la pantalla completa de gestión de círculos [FamiliaScreen.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/presentation/FamiliaScreen.kt):
  - **Selector Dropdown:** Menú flotante de núcleos activos.
  - **Código de Invitación:** Caja interactiva que muestra el código actual con botón de copiado rápido al portapapeles.
  - **Lista de Miembros:** Listado de familiares asociados indicando roles de Admin/Miembro, con botones de expulsión para creadores y opción de abandono de círculo.
  - **Botón de Destrucción:** Opción de eliminar el círculo por completo si el usuario es el dueño original.
  - **Formularios de Círculos:** Tarjetas para crear círculos nuevos o unirse a existentes ingresando un código de 10 caracteres.
  - **Panel de Facturación Premium:** Tarjeta de promoción Premium que lista beneficios, selectores para Stripe/MercadoPago/PayPal y un botón de cobro asíncrono que redirige de forma segura al navegador nativo para finalizar la transacción en la pasarela oficial.

## [2026-07-09] Re-organización de Navegación y Nuevas Secciones (Expo y Android Native)
- **Estoy ok (Re-activado):** Reactivado el panel de bienestar pasivo (ex-"Panel") en el menú inferior renombrándolo como "Estoy ok".
- **Nueva Pestaña "Vehículo":** 
  - **Expo:** Implementada vista en la pestaña `vehicle` mostrando telemetría en tiempo real (estado de conducción, velocidad) y formulario interactivo para guardar límites de velocidad por círculo usando `settingsService.updateCircleSpeedLimit`.
  - **Android Native:** Creada pantalla nativa `VehiculoScreen.kt` mostrando estado de conducción del dispositivo y aclarando el funcionamiento del detector de accidentes y velocidades máximas.
- **Nueva Pestaña "Premium":**
  - **Expo:** Extraído el bloque de checkout de membresía de la pestaña de Ajustes hacia una nueva pestaña dedicada `premium` con la pasarela Stripe integrada.
  - **Android Native:** Creada pantalla nativa `PremiumScreen.kt` usando `FamiliaViewModel` para ofrecer suscripción Premium mediante Stripe, MercadoPago y PayPal, removiéndola de `FamiliaScreen.kt`.
- **Rediseño del Selector de Núcleos (Android Native):** Rediseñado el selector superior de "Núcleo Activo" sobre el mapa de la pantalla nativa `MapaScreen.kt`. Se redujo de una tarjeta de ancho completo a una píldora (combo dropdown) centrada, y se agregaron las opciones directas para "Crear núcleo", "Unirse a núcleo" y "Administrar núcleos" con redirección al tab correspondiente (oculto del menú inferior). El switch de rastreo de fondo se desacopló a una píldora flotante independiente a la derecha.
- **Ocultamiento de Pestaña "Núcleo":** Ocultada la opción "Núcleo" (ex-"Familia") del menú inferior en ambas aplicaciones (Expo y Kotlin Native) dado que las acciones de creación, unión y administración de los núcleos se disparan ahora cómodamente desde el combo superior del Mapa con redirección de navegación directa.
- **Control Seguro de Permisos en Mapa (Android Native):** Corregido el toggle de "Rastreo On / Off" en `MapaScreen.kt` para comprobar si se cuentan con los permisos de geolocalización (`ACCESS_FINE_LOCATION`) y notificaciones (`POST_NOTIFICATIONS` en Android 13+) antes de intentar levantar el servicio. Si faltan permisos, el switch los solicita dinámicamente mediante `RequestMultiplePermissions()` y muestra un Toast si son denegados, previniendo que el servicio aborte prematuramente y crasheé/minimice la aplicación.
- **Panel Desplizable de Miembros en Mapa (Android Native):** Se reemplazó la fila horizontal (`LazyRow`) de miembros sobre el mapa por un panel inferior deslizable e interactivo (Life360 style). Cuando está colapsado, solo muestra una píldora con el integrante seleccionado (o el primero de la lista) optimizando el campo visual del mapa. Mediante un gesto de swipe vertical (o clic en la cabecera/manija), el panel se expande revelando una lista vertical (`LazyColumn`) con todos los miembros y su telemetría en tiempo real (batería, velocidad, señal).
- **Auto-Enfoque Inteligente de Cámara en Mapa (Android Native):** Se programó un `LaunchedEffect` en `MapaScreen.kt` que calcula el contenedor de coordenadas (`LatLngBounds`) de todos los integrantes del núcleo activo. Al cargar el mapa o cambiar de núcleo, la cámara realiza una transición animada suave cubriendo a todo el grupo (Life360 style). Si los integrantes se encuentran muy alejados en distancia, el mapa automáticamente se aleja (zoom out) lo necesario para englobarlos a todos con un margen de holgura (padding 150px) y evitar errores.
- **Botón Flotante de Auto-Enfoque Manual (Android Native):** Se agregó un botón de acción flotante circular (`FloatingActionButton`) en la esquina inferior derecha con el icono de geolocalización. Al hacer clic sobre él, el usuario puede forzar manualmente el auto-enfoque de la cámara del mapa para englobar a todos los miembros de su grupo en cualquier momento si se desplaza manualmente.
- **Ajuste de Padding y Controles en Mapa (Android Native):** Se desactivó el botón de geolocalización por defecto de Google Maps (`myLocationButtonEnabled = false`) para evitar duplicaciones visuales con nuestro botón flotante de auto-enfoque y corregir un bug estético donde dicho control (color violeta por defecto del tema M3) quedaba parcialmente tapado detrás del panel inferior. Además, se configuró `contentPadding` en el mapa con un margen inferior de `120.dp`, logrando que Google Maps posicione automáticamente el logotipo de Google y sus atribuciones reglamentarias por encima del panel inferior deslizable de miembros.
- **Preparación de Entorno Real (Local y USB):** Se revirtió la constante `BASE_URL` en [NetworkModule.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/core/di/NetworkModule.kt) de vuelta a `http://127.0.0.1:8000/api/` para habilitar el desarrollo local. Para conectar el teléfono físico conectado por cable USB al backend local de la computadora, se proveen instrucciones para usar el reenvío de puertos mediante ADB (`adb reverse`).
- **Selector Dinámico de Entornos de Servidor (Android Native):** Se diseñó e implementó un selector segmentado dinámico de servidores en la pantalla de Login (`LoginScreen.kt` y `LoginViewModel.kt`) con dos botones de opción: **💻 Local (USB)** y **🌐 Railway**. La selección se guarda en la persistencia local de `SessionManager` (DataStore), de donde el interceptor de red `AuthInterceptor.kt` extrae y reescribe la URL base de cada petición HTTP en caliente. Esto elimina la necesidad de recompilar la aplicación cada vez que se alterna entre pruebas locales por USB y despliegues en producción de Railway.
- **Tipografía Premium Integrada (Outfit):** Se descargaron e integraron físicamente las familias tipográficas **Outfit** (Regular, Medium, SemiBold, Bold) en los recursos locales del proyecto nativo (`app/src/main/res/font/`). Se redefinió la clase `Typography` en `Type.kt` para utilizar `Outfit` en todos los estilos de texto de la aplicación (títulos, cuerpo, etiquetas), logrando una experiencia de marca premium, moderna y elegante (Life360 style). Al estar los archivos TTF embebidos localmente en la app, la carga de la fuente es instantánea y funciona 100% sin conexión a internet.
- **Persistencia de URL en Cierres de Sesión:** Se corrigió un bug colateral en `SessionManager.kt` donde la función `clearSession()` llamaba a `preferences.clear()`, lo que eliminaba la preferencia de URL de servidor seleccionada cada vez que expiraba un token (401) o se deslogueaba el usuario, provocando rebotes automáticos a la URL por defecto. Ahora `clearSession()` elimina individualmente solo las claves de sesión (`AUTH_TOKEN`, `USER_NAME`, etc.), preservando la selección del entorno.
- **Redirección de Sesiones Expiradas (401 Unauthorized):** Se corrigió un bug en la lógica de autenticación nativa donde la app no detectaba si la sesión/token del usuario había expirado o sido revocado en el backend (por ejemplo, tras desconectar o reiniciar la base de datos). Se actualizó [AuthInterceptor.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/core/data/remote/AuthInterceptor.kt) para interceptar cualquier respuesta HTTP `401` del servidor y borrar inmediatamente las credenciales locales mediante `sessionManager.clearSession()`, lo que gatilla de manera reactiva e instantánea la redirección de vuelta a la pantalla de Login en el teléfono físico.
- **Verificación:** Ejecutada con éxito la compilación TypeScript y pasada la suite de pruebas unitarias/de integración del backend (114/114 tests PASS).

## [2026-07-10] Actualización en Tiempo Real de Chinches de Mapa (Android Native)
- **Corrección de Movimiento en MapaScreen:** Se corrigió el bug en `MapaScreen.kt` que impedía ver el desplazamiento de los familiares en tiempo real mientras se estaba en la pestaña del Mapa.
- **Lógica de Compose:** Se implementó `key(member.id)` y `LaunchedEffect(latLng)` para actualizar reactivamente `markerState.position` al recibir nuevas coordenadas del backend (polling), evitando que la posición del marcador quede estática por el comportamiento de cacheo de `rememberMarkerState`.
- **Verificación:** Ejecutada exitosamente la compilación del código Kotlin (`./gradlew :app:compileDebugKotlin` finalizó con éxito en 2m 15s).
- **Documentación CLI:** Creada la guía de documentación [compilacion_android_cli.md](file:///home/usuario/aplicaciones/estoyok/docs/compilacion_android_cli.md) que explica cómo compilar, construir APKs e instalar la app por USB/ADB directamente desde la terminal.
- **Marcadores Personalizados (Chinches Custom):** Se eliminaron los chinches genéricos de Google Maps. Ahora se utiliza `MarkerComposable` de la librería `maps-compose` junto con `coil-compose` para mostrar avatares circulares con las fotos de perfil de los usuarios (si están subidas al backend en `avatar_url`), o en su defecto sus iniciales centradamente con tipografía destacada y un indicador de estado dinámico (borde verde si está online, gris si está offline, naranja si tiene GPS apagado) y un badge con el primer nombre debajo de cada pin.
- **Resolución de Advertencias del Compilador (Clean Build):** Se corrigieron múltiples advertencias de compilación y elementos obsoletos (deprecados) en los archivos `NavGraph.kt`, `MapaScreen.kt`, `AjustesScreen.kt` y `FamiliaScreen.kt`, reemplazando `Divider` por `HorizontalDivider` y usando los iconos AutoMirrored correspondientes (como `ArrowBack` y `ExitToApp`), además de remover variables y parámetros lambda no utilizados.
- **Detalles de Miembros e Historial de Recorridos (Nativo & Backend):** Se implementó la vista de detalles expandida de familiares en `MapaScreen.kt` usando `ModalBottomSheet`. Cada usuario puede subir/actualizar su propia foto de perfil (almacenamiento público local en Laravel + endpoint `POST /api/settings/avatar`). Se agregó el selector de historial de recorridos, restringiendo a usuarios gratuitos a las últimas 24 horas (con candado y promo premium) y permitiendo 30 días a cuentas Premium. El recorrido se renderiza como `Polyline` con auto-ajuste de cámara en el mapa.
- **Segmentación de Recorridos e Historial (Opción A):** Se implementó la visualización inteligente de sub-viajes/segmentos para evitar líneas cruzadas en el mapa. Las coordenadas se agrupan en viajes separados usando criterios de tiempo (brecha > 10 min) y distancia (brecha > 1.5 km), calculando la duración, distancia (fórmula Haversine) y velocidad máxima estimada para cada tramo. Se muestra la lista de tarjetas de viajes en el `ModalBottomSheet`; al seleccionar un viaje, el panel se oculta y la cámara realiza zoom directamente sobre ese trayecto específico, con controles de navegación ("Atrás" y "Salir") en una barra flotante superior.

## [2026-07-11] Mejoras en Historial de Recorridos (Android Native)
- **Corrección de Scroll en Bottom Sheet:** Se agregó `verticalScroll(rememberScrollState())` al Column de `MemberDetailsSheetContent` para habilitar el scroll del ModalBottomSheet nativo una vez expandido, solucionando la limitación de visualización cuando hay más de 6 viajes.
- **Inversión de Orden de Viajes:** Se cambió la renderización de viajes en el sidebar a `segments.reversed().forEach`, posicionando el viaje más reciente arriba de todo en el listado.
- **Cálculo Exacto de Duración:**
  - Se añadió el fallback de parseo de fechas para el formato plano de la base de datos (`yyyy-MM-dd HH:mm:ss`) en `parseIsoToSeconds` y `formatTime`. Esto resolvió el problema en donde todos los viajes se reportaban con duración de 1 minuto debido a fallos de parseo.
  - Se modificó la duración calculada para estructurarse en formato de texto detallado (`"Menos de 1 min"`, `"X min"` o `"X h Y min"`).
- **Visibilidad de Fechas de Historial para Cuentas Free:** Se reemplazó el candado genérico `🔒 PRO` del carrusel de fechas por la visualización del día y el mes real de cada día anterior del historial, agregando el candado de forma sutil en la solapa del mes (ej. `10 Jul 🔒`) y atenuando el color de texto del día.
- **Verificación:** Ejecutada con éxito la compilación nativa de Gradle (`./gradlew :app:compileDebugKotlin` finalizado con éxito) y los tests de regresión del backend.

## [2026-07-11] Zonas Seguras en App Mobile (Android Native)
- **Implementación CRUD de Geofences en Kotlin:**
  - Añadidas las firmas de método a `CircleRepository.kt` e implementadas en `CircleRepositoryImpl.kt` llamando a los endpoints de Retrofit `createGeofence` y `deleteGeofence` envueltas en `safeApiCall`.
  - Expuestos los métodos CRUD y variables de estado de carga/éxito/error en `MapaViewModel.kt`, sincronizando el núcleo seleccionado (`refreshCircles()`) automáticamente tras el éxito de creación o eliminación.
- **Interacción y Renderizado en MapaScreen (Nativo):**
  - Configurado `onMapLongClick` en `GoogleMap` para capturar coordenadas lat/long interactivamente y abrir un `AlertDialog` nativo de creación de Zonas Seguras.
  - Diseñado un diálogo modal de alta fidelidad que solicita el nombre, posee un slider numérico interactivo para regular el radio (de 50m a 1000m) y un menú dropdown para asignar la Zona Segura a un miembro particular del núcleo o a "Toda la familia".
  - Renderizado automático de Zonas Seguras en el mapa de Google mediante círculos semitransparentes del color primario del tema y pins de marcador celestes (`HUE_AZURE`).
  - Añadido el listado "Zonas Seguras del Núcleo" en el panel deslizable expandido de la home, permitiendo a los administradores del núcleo eliminar cualquier Zona Segura directamente mediante un botón de tacho de basura (`🗑️`).
- **Integración de Línea de Tiempo (Timeline) en Historial:**
  - Desarrollada la clase sellada `TimelineItem` (para `Trip` y `SafeZoneStay`) y la función utilitaria `buildHistoryTimeline(...)`.
  - Integra de forma inteligente y cronológica las estadías del familiar dentro de las Zonas Seguras configuradas entre cada viaje del día (calculando si la última posición de un viaje y la primera del siguiente coinciden con el radio de una Zona Segura mediante la distancia Haversine).
  - Muestra las estadías como tarjetas suaves del color primario (`TimelineItem.SafeZoneStay`) detallando la hora de llegada, salida y la duración exacta de permanencia (ej. "Casa: 10:15 - 12:30 (2 h 15 min)") en la línea de tiempo.
- **Validación y Estabilización:**
  - Verificado el éxito del build y compilación Kotlin (`BUILD SUCCESSFUL` mediante Gradle).
  - Ejecutada la suite completa de pruebas unitarias/de integración del backend con éxito total de no regresión (114/114 passed).

## [2026-07-11] Rediseño de Layout (Life360 Style) e Identidad Electric Teal
- **Esquema de Colores (Electric Teal):**
  - Modificado [Color.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/core/theme/Color.kt) para actualizar el color de marca principal `PrimaryEmerald` a `#00F0C0` (Electric Teal), y `PrimaryTeal` a `#0D9488` (Teal base), logrando un rediseño de marca instantáneo y seguro en todas las pantallas.
- **Rediseño de Controles Flotantes del Mapa (Kotlin Native):**
  - Añadido el botón de acceso rápido a Ajustes en formato de círculo oscuro sombreado con icono de engranaje en la esquina superior izquierda (`Alignment.TopStart`) de [MapaScreen.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/presentation/MapaScreen.kt).
  - Integrados botones rápidos de acción estilo píldora para "Estoy OK" (icono de escudo) y "SOS" (icono de advertencia) flotando sobre el mapa a la izquierda del BottomSheet (`Alignment.BottomStart`).
- **Verificación:**
  - Compilación nativa de Gradle finalizada con éxito (`BUILD SUCCESSFUL` en 37s).
  - Suite de pruebas de no regresión del backend de Laravel pasó con éxito (114/114 passed).

## [2026-07-11] Sincronización de Identidad Cromática en Frontend Web (Next.js)
- **Actualización de Paleta Tailwind CSS v4:**
  - Modificado [globals.css](file:///home/usuario/aplicaciones/estoyok/frontend-web/src/app/globals.css) para redefinir el bloque `@theme` de Tailwind v4, mapeando toda la escala de color `emerald` a los tonos de la nueva identidad corporativa **Electric Teal** (ej. `emerald-500` a `#00f0c0`, `emerald-600` a `#0d9488` y `emerald-700` a `#115e59`).
  - Esto actualiza instantáneamente el color de marca en la landing page, botones del dashboard, mapas de crisis, badges y secciones de facturación del frontend web sin requerir modificaciones dispersas en múltiples archivos TSX.
- **Verificación de Compilación:**
  - Ejecutada exitosamente la compilación del Next.js en el entorno de producción (`docker compose exec frontend npm run build` compiló sin errores en 10.4s).

## [2026-07-11] Corrección de Superposición de Botones en MapaScreen (Nativo)
- **Ajuste de Dibujado en Compose (Relocalización):**
  - Se movieron los botones flotantes ("Estoy OK", "SOS" y "Centrar Grupo") para ser dibujados *después* del Bottom Sliding Card (`Card` de miembros) en la declaración del `Box` en [MapaScreen.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/presentation/MapaScreen.kt). Dado que el card tiene `fillMaxWidth()`, dibujarlos antes causaba que el card se renderizara encima de ellos, ocultándolos por completo.
  - Se aumentó el padding inferior a `145.dp` para posicionar los botones flotando exactamente por encima de la cabecera del panel cuando está colapsado.
  - Se envolvió la fila de acciones y el botón de centrado en un condicional `if (!isExpanded)`. Esto oculta de forma fluida y automática estos botones cuando el panel de miembros se desliza hacia arriba, evitando que obstruyan visualmente el listado de familiares.
- **Verificación:**
  - Compilación nativa completada con éxito (`BUILD SUCCESSFUL`).

## [2026-07-11] Edición de Zonas Seguras y Limpieza de Superposición en Mapa (Nativo)
- **Implementación del CRUD completo de Zonas Seguras:**
  - Añadido el modelo `UpdateGeofenceRequest` y el endpoint `PUT /api/geofences/{geofenceId}` en `CircleApiService.kt`.
  - Implementado `updateGeofence(...)` en el repositorio nativo (`CircleRepositoryImpl`) y el ViewModel (`MapaViewModel.kt`) para actualizar la información de la zona y refrescar los círculos en el mapa automáticamente.
  - Agregado un botón de edición (✏️) en cada ítem de `GeofenceRowItem` en [MapaScreen.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/features/tracking/presentation/MapaScreen.kt) al lado del botón de eliminar.
  - Creado un diálogo de edición nativo (`showEditGeofenceDialog`) que se abre pre-completado con los datos actuales para modificar nombre, radio y asignación a miembros o grupo familiar.
- **Limpieza de Superposición (Ajustes y Brújula):**
  - Desactivadas las propiedades `compassEnabled = false` y `mapToolbarEnabled = false` en `MapUiSettings` del mapa de Google, previniendo que la aguja de la brújula y botones por defecto del mapa aparezcan superpuestos debajo del botón circular de Ajustes (engranaje) en la esquina superior izquierda.
- **Verificación:**
  - Compilación Kotlin nativa finalizada exitosamente (`BUILD SUCCESSFUL`).
  - Suite de pruebas de Laravel completada exitosamente sin regresiones (114/114 passed).

## [2026-07-13] Mejoras en el Historial de Recorridos y Estadías (Estilo Life360)
- **Backend (Laravel):**
    - Modificado `HistoryController.php` para retornar los campos `speed` e `is_driving` en el endpoint del historial `/api/circles/{circle}/members/{member}/history`.
    - Verificado el correcto funcionamiento mediante la suite de tests del backend (114 tests PASS).
- **App Móvil Nativa (Android/Kotlin):**
    - Actualizado `LocationHistoryDto` en `LocationModels.kt` para incorporar las propiedades `speed` e `isDriving`.
    - Implementado el componente Compose reactivo `AddressText` que consulta asíncronamente en segundo plano (`Dispatchers.IO`) el `Geocoder` nativo de Android para resolución gratuita (costo cero) de nombres de calles y lugares.
    - Desarrollado algoritmo local de clasificación de transporte que diferencia entre Caminata (🚶), Bicicleta (🚲) y Vehículo (🚗) según velocidad del tramo y estado de conducción.
    - Diseñado algoritmo inteligente de detección de estadías intercalando cronológicamente permanencias mayores a 5 minutos entre trayectos diarios, y resolviendo automáticamente si ocurrieron dentro de una Zona Segura.
    - Rediseñado visualmente el panel de detalles del historial de miembros en `MapaScreen.kt` para adoptar la estética Life360 con tarjetas estilizadas, iconos grandes, títulos origen-destino en formato de flecha de recorrido, y badges de velocidad y tipo de viaje.
- **Validación:**
    - Compilación nativa completada de forma 100% limpia sin advertencias ni errores (`BUILD SUCCESSFUL`).

## [2026-07-13] Filtrado Inteligente de Micro-Movimientos en Recorridos
- **App Móvil Nativa (Android/Kotlin):**
    - Modificado `segmentHistoryPoints(...)` para descartar caminatas (transportMode == "caminando") con distancia inferior a 150 metros (0.15 km).
    - Implementada lógica de re-indexación secuencial para asegurar coherencia en la interfaz y selección del mapa.
    - Modificado `buildHistoryTimeline(...)` para soportar una estadía unificada de "Todo el día" (00:00 - 23:59) si no hay viajes registrados pero sí coordenadas de presencia.
    - Sincronizados y actualizados los parámetros de `buildHistoryTimeline` y su llamada en `MemberDetailsSheetContent`.
- **Validación:**
    - Compilación nativa Kotlin completada de forma limpia sin advertencias ni errores (`BUILD SUCCESSFUL` en 21s).
    - Suite de pruebas de Laravel completada exitosamente sin regresiones (114/114 passed).

## [2026-07-13] Optimización de Segmentación y Unión de Recorridos Continuos
- **App Móvil Nativa (Android/Kotlin):**
    - Rediseñado el algoritmo de división de segmentos en `segmentHistoryPoints(...)` utilizando lógica física de tiempo, distancia y velocidad combinados.
    - Se eliminó el corte simple de distancia fija (`dist > 1.5 km`) y se reemplazó por:
        - Corte por inactividad prolongada (> 10 minutos).
        - Corte por parada física/estadía confirmada (> 5 minutos y desplazamiento < 200 metros).
        - Corte por salto/error GPS (velocidad terrestre imposible > 180 km/h).
    - Esto evita que los viajes vehiculares con reportes de señal espaciados o de alta velocidad se segmenten incorrectamente.
- **Validación:**
    - Compilación nativa Kotlin completada con éxito (`BUILD SUCCESSFUL` en 19s).
    - Suite de pruebas del backend de Laravel ejecutada y completada sin regresiones (114/114 passed).

## [2026-07-13] Implementación de Tracking Dinámico y Adaptativo (Estilo Life360)
- **App Móvil Nativa (Android/Kotlin):**
    - Creado algoritmo de ajuste dinámico de intervalo de GPS en `TrackingService.kt` mediante `adjustTrackingMode(...)`.
    - Definidos tres estados adaptativos según velocidad y comportamiento:
        - **Vehículo (Speed > 15 km/h):** Intervalo de 10 segundos y distancia mínima de 40 metros.
        - **Caminata (Speed 1.5 - 15 km/h):** Intervalo de 30 segundos y distancia mínima de 15 metros.
        - **Estadía (Speed < 1.5 km/h):** Intervalo de 5 minutos y distancia mínima de 20 metros.
    - Implementada histéresis temporal de 2 minutos para el modo de estadía, evitando caídas de precisión accidentales en semáforos o detenciones breves de tráfico.
    - Habilitada la desconexión del filtro de distancia en actualizaciones de intervalos forzadas por emergencias o alertas activas (fiel a 5 segundos sin restricciones de distancia).
    - Configurado `setMinUpdateDistanceMeters(...)` en `startLocationUpdates()` para aplicar el filtro de movimiento mínimo a nivel del procesador de Google Play Services.
- **Validación:**
    - Compilación nativa Kotlin finalizada exitosamente sin warnings ni errores (`BUILD SUCCESSFUL` en 11s).
    - Suite de pruebas de Laravel completada exitosamente sin regresiones (114/114 passed).

## [2026-07-13] Creación de Zona Segura desde el Historial
- **App Móvil Nativa (Android/Kotlin):**
    - Añadido el callback `onCreateGeofence: (LatLng) -> Unit` a la firma del composable `MemberDetailsSheetContent`.
    - Agregado el botón con icono de escudo (🛡️) en las tarjetas de estadía (`TimelineItem.Stay`) si no coinciden con ninguna zona segura configurada (`matchedGeofence == null`).
    - Vinculada la acción del botón de escudo para disparar el callback `onCreateGeofence(...)` con las coordenadas de la estadía seleccionada.
    - Implementada la recepción del callback en `MapaScreen` asignando las coordenadas a `longClickedLatLng` y activando el diálogo nativo `showCreateGeofenceDialog = true`.
- **Validación:**
    - Compilación nativa Kotlin finalizada exitosamente sin warnings ni errores (`BUILD SUCCESSFUL` en 20s).
    - Suite de pruebas de Laravel completada exitosamente sin regresiones (114/114 passed).

## [2026-07-13] Atajo de Edición de Zona Segura desde el Historial
- **App Móvil Nativa (Android/Kotlin):**
    - Añadido el callback `onEditGeofence: (GeofenceDto) -> Unit` a la firma del composable `MemberDetailsSheetContent`.
    - Agregado el botón con icono de lápiz (✏️) en las tarjetas de estadía (`TimelineItem.Stay`) si coinciden con una zona segura registrada (`matchedGeofence != null`) y el usuario actual es el Administrador (Owner) del Núcleo (`isCircleOwner == true`).
    - Vinculada la acción del botón de lápiz para disparar el callback `onEditGeofence(matchedGeofence)`.
    - Implementada la recepción del callback en `MapaScreen` asignando el objeto a `geofenceToEdit` y activando el diálogo nativo `showEditGeofenceDialog = true`.
- **Validación:**
    - Compilación nativa Kotlin finalizada de forma exitosa sin warnings ni errores (`BUILD SUCCESSFUL` en 18s).
    - Suite de pruebas de Laravel completada exitosamente sin regresiones (114/114 passed).

## [2026-07-13] Ajuste de Zoom Barrial en Mapa (Nativo)
- **App Móvil Nativa (Android/Kotlin):**
    - Modificado el nivel de zoom por defecto al enfocar o centrar un familiar seleccionado en `MapaScreen.kt` de `13.5f` a **`15.5f`** (correspondiendo al zoom barrial de `zoom.jpg`).
    - Modificada la función `fitAllMembers` para ajustar el zoom a `15.5f` cuando hay un solo miembro activo con ubicación.
    - Implementada lógica de proximidad grupal: si la distancia máxima entre todos los familiares es inferior a 1 km, la cámara se centrará en el promedio del grupo a un nivel de zoom fijo de `15.5f` (evitando que `LatLngBounds` haga un acercamiento extremo e incómodo de nivel >20f).
- **Validación:**
    - Compilación nativa Kotlin finalizada con éxito (`BUILD SUCCESSFUL` en 15s).
    - Suite de pruebas de Laravel completada exitosamente sin regresiones (114/114 passed).

## [2026-07-13] Unificación de Botón de Ajustes en Mapa
- **App Móvil Nativa (Android/Kotlin):**
    - Identificada duplicación visual del botón de Ajustes (⚙️) provocado por un renderizado global en `NavGraph.kt` (con padding superior de 8.dp) y uno local redundante en `MapaScreen.kt` (con padding superior de 16.dp).
    - Eliminado el botón flotante local de Ajustes en `MapaScreen.kt` para mantener una única instancia del botón global de `NavGraph.kt`.
- **Validación:**
    - Compilación nativa Kotlin finalizada con éxito (`BUILD SUCCESSFUL` en 15s).
    - Suite de pruebas de Laravel completada exitosamente sin regresiones (114/114 passed).

## [2026-07-13] Estadía en Curso Activa ("Ahora") en Historial
- **App Móvil Nativa (Android/Kotlin):**
    - Creado helper `isTodayLocal(isoStr: String)` para determinar si una fecha UTC corresponde al día de hoy en la zona horaria del dispositivo.
    - Modificado `buildHistoryTimeline` para que, en caso de estar en el día actual, la última estadía (`Stay`) tenga hora de finalización `"Ahora"` en lugar de `"23:59"`.
    - Ajustado el cálculo de duración de la estadía en curso, determinándose dinámicamente hasta la hora actual del sistema (`System.currentTimeMillis()`) y mostrando la etiqueta `"Desde hace [duración]"`.
    - Adaptado el caso sin viajes (persona en casa todo el día de hoy) para que muestre `"Todo el día (hasta ahora)"` con finalización `"Ahora"`.
- **Validación:**
    - Compilación nativa Kotlin finalizada con éxito (`BUILD SUCCESSFUL` en 17s).
    - Suite de pruebas de Laravel completada exitosamente sin regresiones (114/114 passed).

## [2026-07-13] Badges de Movimiento sobre Avatares de Miembros
- **App Móvil Nativa (Android/Kotlin):**
    - Añadido helper `getMovementEmoji(speed: Float?, isDriving: Boolean?): String?` en `MapaScreen.kt` para clasificar desplazamientos en Vehículo (🚗 $\ge 15\text{ km/h}$ o conduciendo), Bicicleta (🚲 $5 - 15\text{ km/h}$) y Caminando (🚶 $1.5 - 5\text{ km/h}$).
    - Modificado el marcador de mapa del miembro (`MarkerComposable`) envolviendo el contenedor de avatar en un `Box` con alineación `Alignment.BottomEnd`.
    - Agregado el badge flotante del emoji del transporte correspondiente sobre la esquina inferior derecha del avatar del marcador con borde blanco y fondo del color primario.
    - Modificada la tarjeta de familiar en la lista deslizable (`MemberRowItem`) aplicando la misma lógica de superposición del badge sobre el avatar.
- **Validación:**
    - Compilación nativa Kotlin finalizada con éxito (`BUILD SUCCESSFUL` en 16s).
    - Suite de pruebas de Laravel completada exitosamente sin regresiones (114/114 passed).

## [2026-07-13] Reportes de Conducción Premium (Backend)
- **Backend (Laravel):**
    - Creado `DriveController.php` con el método `getDrives(Circle $circle, User $member)` para procesar e historizar trayectos vehiculares finalizados de un miembro.
    - Implementada restricción Premium: usuarios en plan gratuito (Free) solo acceden al último trayecto registrado como vista previa, mientras que usuarios Premium acceden a su historial completo de hasta 30 viajes.
    - Creado algoritmo en caliente para el cálculo de distancias (mediante fórmula Haversine entre puntos de telemetría consecutivos) y duraciones de cada trayecto.
    - Implementado detector de eventos de conducción de riesgo analizando diferencias de telemetría temporal de GPS:
        - **Frenada Brusca (Hard Brake):** Descenso de velocidad $\ge 15\text{ km/h}$ en $\le 3\text{ segundos}$.
        - **Aceleración Rápida (Rapid Acceleration):** Incremento de velocidad $\ge 12\text{ km/h}$ en $\le 3\text{ segundos}$.
        - **Exceso de Velocidad (Speeding):** Comparativa del punto con el límite de velocidad del círculo.
    - Implementado score de seguridad (`safety_score`) de 0 a 100 con descuentos ponderados por eventos de riesgo ocurridos durante el trayecto.
    - Creada suite de pruebas feature `DriveReportsTest.php` validando la autorización de rutas, restricción Premium, geolocalización de eventos y cálculo matemático del score de seguridad.
- **Validación:**
    - Suite de pruebas de Laravel completada exitosamente sin regresiones (119/119 passed).

## [2026-07-13] Corrección de Anclaje de Marcadores en Mapa
- **App Móvil Nativa (Android/Kotlin):**
    - Rediseñado el layout de `MarkerComposable` en `MapaScreen.kt` para ubicar el nombre del usuario arriba del avatar y la punta del piquito indicador en la base inferior absoluta.
    - Se eliminaron los offsets negativos que desplazaban las dimensiones del layout real, logrando que el anclaje predeterminado de Google Maps `(bottom-center / 0.5f, 1.0f)` apunte con total precisión sobre la coordenada de la calle.
- **Validación:**
    - Compilación nativa Kotlin finalizada con éxito (`BUILD SUCCESSFUL` en 13s).
    - Suite de pruebas de Laravel completada exitosamente sin regresiones (119/119 passed).

## [2026-07-14] Seguimiento Online Continuo y Deslizamiento de Marcadores
- **App Móvil Nativa (Android/Kotlin):**
    - Implementada animación fluida en `MapaScreen.kt` para interpolar la posición del marcador (`markerState.position`) a lo largo de 1500 ms cuando se reciben nuevas coordenadas por el canal en tiempo real.
    - Esto elimina el comportamiento de teletransportación y permite ver al marcador deslizarse suavemente sobre las calles y rutas al seguir en vivo a un familiar.
- **Web Frontend (Next.js):**
    - Creado el subcomponente `<AnimatedMarker>` en `CircleMap.tsx` y `EmergencyMap.tsx` encapsulando la API de `requestAnimationFrame` de React para realizar transiciones de interpolación lineal fluidas en mapas de Leaflet.
- **Validación:**
    - Verificación exitosa de compilación nativa en Kotlin (`BUILD SUCCESSFUL`).
    - Verificación exitosa de compilación del frontend Next.js en producción (`Compiled successfully`).
    - Suite completa de 119 pruebas del backend Laravel superada con éxito (119/119 tests PASS).

## [2026-07-14] Rediseño Visual de Zonas Seguras en Mapa
- **App Móvil Nativa (Android/Kotlin):**
    - Eliminado el marcador `Marker` (chinche celeste) del interior del bucle de geocercas en `MapaScreen.kt` para evitar la sobrecarga y el ruido visual sobre la interfaz del mapa.
    - Se mantiene únicamente el componente `Circle` el cual se dibuja usando el color primario del tema (`MaterialTheme.colorScheme.primary`), que corresponde al Turquesa Eléctrico de la marca Estoy Ok.
- **Web Frontend (Next.js):**
    - Modificado `getGeofenceColor` en `CircleMap.tsx` para que retorne siempre `#00E5D9` (Turquesa Eléctrico) unificando la estética de la plataforma.
- **Validación:**
    - Verificación exitosa de compilación nativa Kotlin (`BUILD SUCCESSFUL`).
    - Verificación exitosa de compilación Next.js (`Compiled successfully`).

## [2026-07-14] Estabilización de Insignias de Movimiento (Badges de Tránsito)
- **App Móvil Nativa (Android/Kotlin):**
    - Rediseñados los contenedores Box del avatar tanto en el marcador del mapa (`MarkerComposable`) como en la lista de familiares (`MemberRowItem`) para usar anchos y altos fijos mayores (`56.dp` por `50.dp` en mapa y `48.dp` por `44.dp` en lista).
    - Esto elimina el uso de `.offset()` que desplazaba las vistas fuera de los límites lógicos medidos por el motor de diseño, evitando recortes o desapariciones en el canvas del mapa.
    - Modificado `getMovementEmoji(...)` para que evalúe y retorne la insignia de movimiento según el estado actual (caminata 🚶, bicicleta 🚲, vehículo/conducción 🚗) y retorne `null` (ocultando el badge por completo) si el usuario está quieto, respetando la privacidad del reposo.
- **Validación:**
    - Verificación exitosa de compilación nativa Kotlin (`BUILD SUCCESSFUL`).

## [2026-07-14] Telemetría y Estadía en Tiempo Real sobre Marcadores de Mapa
- **App Móvil Nativa (Android/Kotlin):**
    - Creado un estado reactivo local `stayTracker` (`mutableStateMapOf`) en `MapaScreen.kt` que registra las coordenadas iniciales y la marca de tiempo de entrada al modo reposo de cada miembro.
    - Implementada lógica de comparación de distancia geodésica mediante la fórmula Haversine: si un miembro permanece a menos de 30 metros de su última posición registrada, se mantiene el tiempo de inicio de su estadía; si se desplaza más allá de ese radio o empieza a conducir, el tracker se reinicia.
    - Rediseñada la tarjeta de etiqueta de nombre (`Card`) en la parte superior del marcador de mapa (`MarkerComposable`) incorporando una vista en columna: muestra el primer nombre del usuario en negrita (`9.sp`) y un subtítulo secundario en color turquesa (`7.5.sp`, negrita) con la velocidad actual si está conduciendo (ej. `65 km/h`) o la duración exacta de su estadía en el lugar (ej: `45 min`, `2h 15m`).
- **Validación:**
    - Verificación exitosa de compilación nativa Kotlin (`BUILD SUCCESSFUL`).

## [2026-07-14] Reubicación de Control de Rastreo y Auto-Inicio
- **App Móvil Nativa (Android/Kotlin):**
    - Removida la tarjeta flotante de control de servicio de rastreo (`Rastreo On/Off`) en `MapaScreen.kt` para simplificar la interfaz gráfica del mapa.
    - Agregado el control de servicio de ubicación a la pantalla de Ajustes (`AjustesScreen.kt` / `AjustesViewModel.kt`) bajo un nuevo bloque "Ubicación en Tiempo Real", gestionando permisos y estados en segundo plano de forma coherente.
    - Implementada lógica de auto-inicio en `MapaScreen.kt` mediante un `LaunchedEffect(Unit)`: si la aplicación cuenta con permisos de geolocalización y notificaciones aprobados por el usuario, el servicio de rastreo se inicia de forma silenciosa por defecto al abrir la pantalla de mapa.
- **Validación:**
    - Verificación exitosa de compilación nativa Kotlin (`BUILD SUCCESSFUL`).

## [2026-07-14] Despliegue Automático de Recorridos en Click de Marcador
- **App Móvil Nativa (Android/Kotlin):**
    - Modificado el callback `onClick` de `MarkerComposable` en `MapaScreen.kt` para que, en lugar de retornar `false` (que abría un popup de Info Window nativo redundante), asigne `selectedMemberForMap = member` y `viewModel.selectedMember = member`, retornando `true` para consumir el evento.
    - Esto desencadena directamente la visualización de `ModalBottomSheet` con el panel `MemberDetailsSheetContent`.
    - Agregado un `LaunchedEffect(member.id)` en `MemberDetailsSheetContent` que detecta la carga del panel del familiar y automáticamente dispara `viewModel.loadMemberHistory(member.id, todayStr)` con la fecha de hoy, cargando los recorridos y paradas inmediatamente en pantalla.
- **Validación:**
    - Verificación exitosa de compilación nativa Kotlin (`BUILD SUCCESSFUL`).

## [2026-07-14] Corrección de Caché de Avatar y Validación de Tamaño de Foto
- **App Móvil Nativa (Android/Kotlin):**
    - Añadida la propiedad `avatarVersion` al `MapaViewModel.kt`, incrementándose en cada carga de avatar exitosa.
    - Modificados los componentes `AsyncImage` en `MapaScreen.kt` (mapa, lista e historial de detalles) para adjuntar un parámetro cache-buster (`?v=version`) al URL de la foto si pertenece al usuario logueado. Esto elude el caché agresivo de Coil y muestra de forma inmediata la nueva imagen.
    - Implementada validación local de tamaño de archivo en `imagePickerLauncher`: antes de convertir y enviar la imagen, verifica que `fileBytes.size` sea $\le 2$ MB. Si excede el tamaño, interrumpe el flujo y muestra un Toast amistoso informando al usuario.
- **Validación:**
    - Verificación exitosa de compilación nativa Kotlin (`BUILD SUCCESSFUL`).

## [2026-07-14] Eliminación de Nombre Redundante en Marcador de Mapa
- **App Móvil Nativa (Android/Kotlin):**
    - Modificado el diseño del marcador en `MapaScreen.kt` para remover la visualización del nombre de pila del usuario por encima de su círculo de avatar, eliminando información redundante en el mapa.
    - Se mantuvo la tarjeta flotante superior del marcador únicamente para mostrar la telemetría (velocidad en auto) o la duración de la estadía si el usuario está quieto, apareciendo de forma condicional solo cuando hay datos válidos.
- **Validación:**
    - Verificación exitosa de compilación nativa Kotlin (`BUILD SUCCESSFUL`).

## [2026-07-14] Consistencia de Avatares en Pantalla de Vehículo
- **App Móvil Nativa (Android/Kotlin):**
    - Modificado el componente `AsyncImage` en el selector horizontal de familiares en `VehiculoScreen.kt` para concatenar el cache-buster versionado (`?v=version`) al URL del avatar del usuario actual. Esto evita la entrega de imágenes desactualizadas o rotas por la caché de Coil tras cambiar la foto de perfil en la aplicación.
- **Validación:**
    - Verificación exitosa de compilación nativa Kotlin (`BUILD SUCCESSFUL`).

## [2026-07-14] Robustez y Fallback de Iniciales en Carga de Avatar
- **App Móvil Nativa (Android/Kotlin):**
    - Se reemplazó el uso de `AsyncImage` por `SubcomposeAsyncImage` en todos los avatares (Marcador de Mapa, Lista de Familiares, Detalles de Miembro y Selector de la Solapa Vehículo).
    - Implementado un bloque `error = { ... }` que dibuja localmente las iniciales del usuario si falla la conexión de red, si la imagen no existe en el servidor (404), si la base de datos devuelve valores basura como la cadena `"null"`, o si las URLs apuntan a un localhost inaccesible por cable de red físico desde el servidor de Railway.
- **Validación:**
    - Verificación exitosa de compilación nativa Kotlin (`BUILD SUCCESSFUL`).

## [2026-07-14] Resolución Dinámica de URLs de Avatares (Múltiples Entornos)
- **Backend (Laravel/PHP):**
    - Modificado el accessor `getAvatarUrlAttribute` en `User.php` para que, en caso de existir una petición HTTP activa (`request()->httpHost()`), construya la URL absoluta utilizando dinámicamente el esquema y host de dicha petición (`request()->schemeAndHttpHost()`).
    - Esto anula la dependencia del valor estático `APP_URL` de la configuración local en el archivo `.env` (que típicamente vale `http://localhost` y causaba que los teléfonos físicos intentasen descargar avatares de sí mismos en Railway), garantizando enlaces plenamente válidos (`https://.../storage/avatars/...`) en producción y staging.
- **Validación:**
    - Suite de pruebas completa del backend ejecutada exitosamente (`119 passed, 390 assertions`).

## [2026-07-14] Detección de Proxy Reverso HTTPS y Estado de Carga de Iniciales
- **Backend (Laravel/PHP):**
    - Añadida detección explícita del encabezado `X-Forwarded-Proto` en `getAvatarUrlAttribute` de `User.php`. Esto previene que el proxy reverso del servidor (como el de Railway) obligue a usar el protocolo inseguro `http://` (el cual es abortado por defecto por la política de seguridad Cleartext de Android), forzando correctamente un enlace seguro `https://` en entornos de producción.
- **App Móvil Nativa (Android/Kotlin):**
    - Agregado el parámetro `loading = { ... }` a los componentes `SubcomposeAsyncImage` en `MapaScreen.kt` y `VehiculoScreen.kt`. Esto dibuja las iniciales del miembro de forma inmediata durante el transcurso de la petición de red, erradicando estados de pantalla vacíos y asegurando una excelente retroalimentación visual al usuario en redes móviles.
- **Validación:**
    - Verificación exitosa de compilación nativa Kotlin (`BUILD SUCCESSFUL`).
    - Suite de pruebas completa del backend ejecutada exitosamente (`119 passed, 390 assertions`).

## [2026-07-14] Enlaces Simbólicos Relativos para Almacenamiento en Railway
- **Backend (Laravel/PHP):**
    - Modificado `railway.json` para eliminar el enlace simbólico obsoleto y ejecutar `php artisan storage:link --relative` en la etapa de pre-despliegue. Esto evita los enlaces simbólicos absolutos que causaban errores HTTP 404 al apuntar a directorios de la fase de compilación (`/workspace/...`) inexistentes en el contenedor de ejecución (`/app/...`).
    - Añadido el paquete de Composer `symfony/filesystem` al proyecto (ejecutando `composer require symfony/filesystem`) para proveer soporte nativo de enlaces simbólicos relativos en el framework de Laravel.
- **Validación:**
    - Verificación exitosa de creación de enlace simbólico relativo local (`rm -rf public/storage && php artisan storage:link --relative` ejecutado correctamente dentro del contenedor local).
    - Suite de pruebas completa del backend ejecutada exitosamente (`119 passed, 390 assertions`).

## [2026-07-14] Reubicación del Enlace Simbólico al Comando de Arranque
- **Backend (Laravel/PHP):**
    - Se trasladó la lógica de creación de enlace simbólico relativo (`rm -rf public/storage && php artisan storage:link --relative`) del `preDeployCommand` al `startCommand` en `railway.json`.
    - Esto se debe a que en Railway, el preDeployCommand se corre en un contenedor efímero temporal y los cambios de archivos allí generados no se heredan en el contenedor de ejecución final. Al correrlo al inicio del `startCommand`, garantizamos que la carpeta `public/storage` exista dentro del contenedor final que sirve las peticiones HTTP.
- **Validación:**
    - Suite de pruebas completa del backend ejecutada exitosamente (`119 passed, 390 assertions`).

## [2026-07-14] Pre-descarga Concurrente de Avatares a Bitmap a Nivel de Pantalla (MapaScreen)
- **App Móvil Nativa (Android/Kotlin):**
    - Se implementó un estado reactivo asociativo `markerBitmaps` (`SnapshotStateMap<Int, Bitmap>`) y un `LaunchedEffect(viewModel.selectedCircleMembers, viewModel.avatarVersion)` a nivel superior de la pantalla `MapaScreen.kt`.
    - Esto pre-descarga de forma segura y paralela los avatares de todos los miembros del núcleo a memoria como objetos `Bitmap` no-hardware (evitando el ciclo de renderizado asincrónico dentro del sub-árbol de `MarkerComposable`).
    - Se reemplazó el casteo explícito de `BitmapDrawable` por la función de extensión universal `drawable.toBitmap()` de `core-ktx` para soportar correctamente los drawables con animaciones de Coil (`CrossfadeDrawable`).
    - Se configuró la descarga para lanzar hilos de ejecución concurrentes en paralelo (`launch { }`) por cada miembro del núcleo. Esto previene que URLs de localhost muertas u obsoletas (comunes en pruebas locales previas) causen bloqueos secuenciales de timeout (de 10 a 15 segundos) al descargar el resto de los avatares válidos de producción.
    - Los marcadores simplemente leen del mapa indexado por ID de miembro (`markerBitmaps[member.id]`) y, en cuanto el bitmap se encuentra disponible en memoria, redibujan instantáneamente el avatar mediante una vista `Image` convencional sin retardos.
- **Validación:**
    - Verificación exitosa de compilación nativa Kotlin (`BUILD SUCCESSFUL`).

## [2026-07-16] Corrección de Pérdida de Rastreo en Segundo Plano y UX de Permisos
- **App Móvil Nativa (Android/Kotlin):**
    - Se agregaron logs detallados (`Log.d`/`Log.e`) a `TrackingService.kt` y `LocationRepositoryImpl.kt` para simplificar la depuración con `adb logcat`.
    - Se solucionó la cola de ubicaciones offline local (Room): ahora, cuando el dispositivo reporta una coordenada en vivo con éxito, se invoca `flushOfflineQueue()` de forma reactiva en segundo plano para subir todas las coordenadas acumuladas por pérdida de red durante traslados.
    - Se implementó un diálogo de advertencia prominente ("Prominent Disclosure") para el permiso de ubicación en segundo plano (`ACCESS_BACKGROUND_LOCATION`) tanto en `MapaScreen.kt` como en `AjustesScreen.kt`. Si el usuario tiene el permiso configurado solo en "Con la app en uso", se le explica la importancia del rastreo en segundo plano y se le redirige de forma amigable a la pantalla de Ajustes del sistema para activar "Permitir todo el tiempo".
- **Validación:**
    - Se verificó la compilación de la app nativa en Kotlin exitosamente (`BUILD SUCCESSFUL`).

## [2026-07-17] Estabilización de Rastreo (Filtros de Deriva) y Auto-inicio de Servicio en Boot
- **App Móvil Nativa (Android/Kotlin):**
    - Implementados filtros de precisión (`accuracy > 40f`) y desplazamiento de deriva (`distance < 15f` con velocidad `< 3 km/h`) en `processLocationUpdate` de `TrackingService.kt`. Esto evita que el marcador de mapa rebote o muestre falsos movimientos cuando el usuario está quieto o en interiores (drift del GPS), alineando el comportamiento al de Life360.
    - Creado [BootReceiver.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/services/BootReceiver.kt) y registrado en [AndroidManifest.xml](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/AndroidManifest.xml) para capturar los eventos `BOOT_COMPLETED`, `QUICKBOOT_POWERON` y `MY_PACKAGE_REPLACED`, garantizando el reinicio automático del servicio de rastreo si el dispositivo se reinicia o se actualiza.
    - Persistido el estado de activación de rastreo mediante `saveTrackingEnabled` en [SessionManager.kt](file:///home/usuario/aplicaciones/estoyok/android-native/app/src/main/java/com/estoyok/app/core/data/local/SessionManager.kt), e inyectado en `AjustesViewModel.kt` y `MapaViewModel.kt` para sincronizar y condicionar reactivamente el inicio y auto-inicio del servicio de localización respetando las preferencias del usuario.
    - Implementada la exclusión de optimización de batería (Whitelisting): declarado el permiso `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` y añadido diálogo prominente interactivo en `MapaScreen.kt` para solicitar al usuario configurar el uso de batería de la app como "Sin Restricciones" (evitando la suspensión de servicios en segundo plano por el sistema operativo).
    - Agendado y configurado el worker de sincronización `LocationSyncWorker` en `EstoyOkApplication.kt` con WorkManager utilizando restricciones de red conectada (`NetworkType.CONNECTED`), garantizando el vaciado automático de la cola Room local al recuperar la red.
    - Implementada la resiliencia ante cierres forzados: sobreescrito `onTaskRemoved` en `TrackingService.kt` para programar un reinicio del servicio mediante `AlarmManager` en 5 segundos si el usuario desliza y fuerza el cierre de la app desde la pantalla de recientes.
    - Integrado el sensor de hardware `Sensor.TYPE_SIGNIFICANT_MOTION` en `TrackingService.kt` para despertar y acelerar instantáneamente el GPS de su estado de reposo lento (Estadía) a modo activo en cuanto se detecta movimiento, optimizando la batería sin sacrificar precisión al iniciar trayectos.
    - Implementada la dispersión de marcadores superpuestos: desarrollado un algoritmo de dispersión circular geométrica (Spiderfying) en `MapaScreen.kt` para desfasar en círculo (aprox. 15 metros) los avatares que estén a menos de 20 metros de distancia física, permitiendo ver a todos individualmente.
- **Validación:**
    - Se verificó la compilación de la app nativa en Kotlin exitosamente (`BUILD SUCCESSFUL`).
